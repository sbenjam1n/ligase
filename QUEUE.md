<!-- QUEUE.md — shared work-ordering for the ligase~ project (just the user + Claude).
§1 is the single source of truth for "what to work on next." This file ORDERS; it does
not DUPLICATE — each item points to its plan/tracking note, and the pointed-to doc wins on
conflict. Convention: bump **Queue Seq** and add a one-line §6 history entry on any change
to §1. Section numbers are stable references — append, never renumber. -->

# QUEUE.md — Work Queue

**Queue Seq:** 95
**Date:** 2026-07-06
**What this queue draws from:** active execution plans (`Plans/*.md`) and the project's own indexes (`README.md`, `TODO.md`).

---

## §0. STRATEGIC MAP — where the project is, where it's going

ligase~ is a Pure Data granular synthesizer/sampler/looper/delay external (C, GPL-v2). Two
sessions — 2026-06-16 (runtime-bug + docs pass) and 2026-06-19→22 (delay/stut/bencina overhaul) —
fixed every reported issue. All work is **implemented and verified headless**, was on
`fix/audio-engine-and-manual`, and is now **merged into `main`** (2026-06-22, fast-forward, 74
commits). Nothing is left to code on reported items — remaining is **user hardware/ear sign-off**.
The 2026-06-19→22 work (B13–B35) added: the envelope-type-change crash fix; delay no-wet /
instability / headless-0 / init-wipe fixes; the full signal-driven Stut model (count/reduction/
spacing mapped from the shared delay inlets, layering voice pool, length system); DD-4 `delay_glide`;
the Bencina granular-cloud rebuild (per-grain position scatter + random pan, with `bencina_spread`/
`bencina_edge`/`bencina_level`/`bencina_pan` controls + tanh-limited makeup); and the Makefile
header-dependency fix (B26) that had been silently masking earlier work.

The 2026-07-04→06 session (Seq 47-79) then built the FEATURE layer on top: the pattern subsystem
(mini-notation + cycle clock + modulation/pitch/events), the pitch-destination arc (P1-P3), morph/
metasurface + snapshots, polyphony, spatial granulation, the modulation matrix (v1 + per-grain
v1.5 with capture transparency), the resonator bank, the snapshot expander (cold-edit buffer,
schema v3/v4), source shapes, scope taps (outlets 10/11 + the grain constellation), and the
control surface made REAL: one layout source (`docs/ui/panel_layout.py`) emitting the SVG
silkscreen, the working `pd/ligase_panel.pd`/`pd/ligase_xpndr.pd` patches, and the
`dist/ligase.plugdata` install bundle (`make bundle`). Seq 61-70 are merged to `main`; Seq 71-78
sit on `claude/queue-execution-plans-8snsz1`. Next horizon: owner hands-on gates, then the VST
arc (`Plans/vst_plugin.md`, license deferred at Seq 77).

| Objective | Status | Source |
|-----------|--------|--------|
| **B1 — Sample-rate-agnostic engine & consistent buffering** | DONE (Steps 1+2); Tier-2 audible = user | `Plans/sample_rate_buffering.md` |
| **B2 — Reel load/save on macOS** | DONE (canvas paths + robust parser + 16-bit + error routing) | `Plans/reel_io_macos.md` |
| **Record modes (B4 recsplice SOS-VCA, B5 overdub TLA)** | DONE — hardware-confirmed | §1 |
| **Runtime stability (B6 denormals, B7 fftease symbol collision, B8 CPU runaways)** | DONE; long-session/coexistence sign-off = user | §1 |
| **Fog default retune (musical)** | DONE; ear-test = user | §1 / §6 Seq 18 |
| **B9 — Fog wet/dry level match (volume bug)** | DONE (removed cancelling phase filter; per-frame energy normalization); ear-test = user; known small residual dips on tonal material | §1 (B9) / §6 Seq 20–21 |
| Manual: single source + `make manual` + content/SOS/modulation accuracy | DONE | `Plans/pdf_manual_regeneration.md`, `Plans/manual_content_edits.md` |
| Stale-artifact / build-naming cleanup (`erosion` leftovers) | BACKLOG | §4 |
| Core external (build, fog, modulation engine) | DONE | `README.md`, `TODO.md` |

## §0.5 ACTIVE INDEXES (fixed pointer — do not catalogue here)

- `README.md` — feature overview, build, install, signal flow.
- `TODO.md` — change log (all items `✓ DONE`); the "don't change" notes.
- `AUTOMATED_TEST_PROCEDURE.md` — audio-routing regression test (Linux).
- `TEST_PLAN_MACOS.md` — macOS + Focusrite test plan (SR sweep, buffering, reel I/O); verifies B1/B2.
- `Plans/` — execution plans.

## §1. WORK LOG — all items COMPLETE (this table is now the changelog)

**ACTIVE QUEUE: (none.)** Every B/M item in the table below is implemented and verified headless,
committed, and merged into `main`. No agent-actionable code work remains on reported items — what's
left is **user hardware/ear sign-off** (not agent work) plus the two open stubs tracked elsewhere:
the §2 manual-content remainder and the §4 build-naming cleanup. The completed B/M rows are retained
here as the project changelog (newest fixes — B13–B35 — are the 2026-06-19→22 delay/stut/bencina arc).

_Branch `fix/audio-engine-and-manual` PUSHED to origin (2026-06-18, as owner). The FFT fog (B9) was removed entirely and replaced by the allpass smear (Seq 29–30); owner ear-tested it positively (resonator/bell mode works). The "silence/CPU after recording a splice" reports (B10/B11) were ultimately a STALE plugdata binary loading the old fog build — resolved once the smear build was actually loaded; recsplice then got a REAL bug fixed (B12, Seq 31): it overdubbed the current splice + left a quiet wrong copy in the new splice, because the last splice's end tracks reel->length, so the take grew the current splice and the granulator fed back on its own recording. Fixed by planting the new-splice boundary marker at recsplice START (pins the current splice's end) + honoring the existing `jump_to_new` flag on stop. Verified headless. Other items (B1/B2/B4/B5/B6/B8) + the smear ear-test remain at owner hardware sign-off._

| # | Item | Status | Gate to stop at | Plan |
|---|------|--------|-----------------|------|
| B1 | Sample-rate-agnostic engine & consistent buffering (delay dead + clicks/dropouts off-48k) | **Steps 1+2 DONE; Tier-2 (user) pending** | Step 1 (delay/distortion/bencina SR re-init) + Step 2 (reel capacity reallocs to host rate via `reel_set_sample_rate`; WAV header writes real rate; load accepts any rate) DONE. Headless Tier-1 PASS at 44.1/48/96 kHz: delay tap correct, distortion bounded, reel duration consistent + header rate correct, 96 k round-trip intact. Remaining: Tier-2 audible Focusrite check (user). | `Plans/sample_rate_buffering.md` |
| B2 | Reel load/save on macOS | **DONE (2026-06-16)** | Core: `load`/`save` resolve via the patch canvas (not Pd's CWD) — verified cwd≠patchdir. Polish: robust chunk-walking WAV parser (tolerates extra chunks / non-16 fmt / cue-after-data), 16-bit PCM accepted (→float), read/write checks, and specific failures routed through `pd_error` (`reel_io_strerror`). Verified headless: float round-trip (w/ cue), 16-bit load, and clear errors for missing/non-WAV files. | `Plans/reel_io_macos.md` |
| B3 | "Empty splices" report (OVERDUB silent at high SOS) | **CLOSED — NOT A BUG (reverted)** | OVERDUB `input×(1−sos)` is DESIGNED SOS attenuation (incl. initial pass — confirmed by owner 2026-06-16). My virgin-territory "fix" altered designed SOS behavior and was fully reverted (`reel.c`/`types.h` back to original). | (n/a) |
| B5 | Overdub Time Lag Accumulation | **DONE — hardware-confirmed (2026-06-16)** | Record head loops within current splice + records granular playback fed back (SOS-coeff capped 0.95 + ±1 feedback clamp + tanh ceiling). Owner confirmed "expected splice behavior" on the Focusrite. `TLA_FEEDBACK_MAX` tunable. | (inline; `test_tla.pd`) |
| B4 | `recsplice` (NEW_SPLICE) bypassed the SOS VCA | **FIXED (2026-06-16)** | NEW_SPLICE was lumped with INPUT_ONLY (forced full-replacement of raw input), so SOS didn't act as a VCA there — contradicting the design ("recinput is the only non-VCA mode") and the manual. Fix: the two `sos_mode==1` record sites in `ligase~.c` now copy `out_left/out_right` (the SOS-mixed monitor signal) for NEW_SPLICE; `recinput` raw + `sos_mode==0` untouched. Verified: recsplice sos 0/0.5/1 → 0.00/0.41/0.58 (constant-power VCA); recinput raw 0.58; overdub `1−sos` unchanged. | (inline; `test_rec.pd`) |
| B12 | `recsplice` behaved like overdub (TLA into current splice) + quiet wrong copy in the new splice | **FIXED (2026-06-18) — owner ear-test pending** | Root cause: `splice_get_bounds` makes the LAST splice's end = `reel->length` (`splice.c:95`); after any take `current_splice` is the last splice, so as recsplice extends the reel the current splice GREW to swallow the tape being recorded → the granulator read its own freshly-recorded `out_*` → feedback = overdub/TLA, and the new marker carved at stop held the SOS-attenuated tail (the "quiet wrong copy"). recinput is immune (records raw input, no granular in the loop); overdub is meant to loop. Fix (`ligase~.c`): plant the new-splice boundary marker at recsplice **start** (`ligase_rec_splice`) so the current splice's end is pinned at the take's start; keep granulating the existing splice during the take; on stop honor the existing **`jump_to_new`** flag (default 0=stay; 1=switch + reposition playhead, same path as `ligase_add_splice`). recinput was then FULLY unified with recsplice (Seq 32 = shared `splice_jump`; Seq 33 = recinput also plants its marker at START so the playing splice keeps playing through the take). Final state: BOTH modes pin the current splice at start, BOTH honor `splice_jump` (0=stay default, 1=jump+reposition), differing ONLY in what's recorded (recinput=raw input, recsplice=SOS monitor mix). overdub + monitor chain untouched. Verified headless: recsplice current splice pinned `0–16768` while reel grew to `77760`; recinput current splice pinned `0–16832` while reel grew to `48512` (loop keeps playing); 2×2 `splice_jump` matrix consistent across both modes; overdub creates no splice. | (inline; `/tmp/test_recsplice_{stay,jump}.pd`, `/tmp/t_recinput_pin.pd`, `/tmp/t_{recinput,recsplice}{0,1}.pd`) |
| B13 | Random crash (SIGSEGV) in `envelope_sample` ← `grain_delay_bencina_process`, after changing envelope type while using Bencina delay | **FIXED (2026-06-19) — owner verify** | Owner crash report (plugdata 0.9.2, x86): EXC_BAD_ACCESS in `envelope_sample`+95 via `grain_delay_bencina_process`. Faulting addr decoded from the crashing `mulss xmm1,[rax+rcx*4+4]` (rax=table=0x04000000, rcx=idx≈795903) = a ~795k OOB index → `env->length` was garbage → **dangling envelope pointer**. Root cause: `ligase_envelope` (envelope-type message) did `envelope_destroy(x->envelope); x->envelope = envelope_create(...)` and updated `x->scheduler->envelope` BUT NOT `x->delay_bencina->envelope` (the Bencina delay caches the envelope pointer from create time). So any envelope-type change while Bencina was/became active left Bencina reading a freed envelope → use-after-free (random, depends on freed-block reuse). Fix: added `envelope_set_type()` that rebuilds the table IN PLACE (same struct/allocation, like `envelope_set_skew`, which already runs from the audio thread), and changed the handler to call it instead of free+recreate. The envelope struct pointer is now stable for its whole lifetime → NO holder (scheduler, Bencina, or future) can dangle; skew preserved automatically. Only runtime `envelope_create`/`destroy` now are ctor/dtor. Verified headless: 6 s of Bencina delay + rapid envelope-type cycling → clean exit (was the exact crash path). | (inline; `src/envelope.c`, `ligase~.c`; `/tmp/t_env_bencina.pd`) |
| B14 | Bencina & Stut delay modes produce no wet (only DD-4 works; mix acts as an attenuator) | **FIXED (2026-06-19) — owner verify** | Owner: only DD-4 has effect; Bencina/Stut "not functioning," wet/dry just attenuates. Isolated each mode in a standalone harness (measured wet at mix=1). TWO independent bugs. (1) **Bencina**: grains read `write_head − delay_time·sr`, but the default **splice-wrap** remapped the read into REEL splice coordinates to index the *delay buffer* (a separate 9.5 s circular line) — for any splice not starting at buffer pos 0 it read unwritten/silent regions → 0 wet. Compounded: `trigger_bencina_grain` HARDCODED `wrap_mode=1`, so `bencina_wrap` was inert and splice-wrap was always on. Fix: wrap in delay-buffer space relative to the write head (global = straight delay; loop = confine to the most recent splice_length samples); added `default_wrap_mode` (def 0=global) that trigger honors + setter sets. Also needs delay_time>0 (0 = no offset = silent, inherent). (2) **Stut**: emitted a single-sample impulse per repeat → ~0 wet energy (mix just attenuated). Fix: each repeat now REPLAYS the captured slice (length = repeat spacing, the audio just before the trigger) with an edge fade — a real stutter; no new param (still count/feedback/time). Verified headless: all 3 modes produce wet incl. offset reel splices (Bencina 0→1.06, Stut 0→0.82). Manual updated (wrap default 0, stut slice replay). | (inline; `src/grain_delay_bencina.c`, `grain_delay_stut.c`, `types.h`) |
| B15 | Delay w/ high feedback/repeats on splices that contain delay → dropouts / sub-meter volume / blow-up→silence | **FIXED (2026-06-19) — owner verify** | Owner: high feedback/repeats, esp. on splices that already contain delay, intermittently → dropouts, volume the meter doesn't show, or blow-up then silence. Audited the firewalls: delay buffers (±4 clamp + NaN flush), Moog (per-stage clamp+SANITIZE — resonant, self-heals), smear (flush), final out (±1 + NaN→0) are all protected. **Gap: the reel recording write (`reel.c recorder_process`) had NO clamp and NO NaN flush.** NEW_SPLICE records the SOS monitor (up to ±4 when delay is hot); that hot content baked into the splice, and re-granulating (overlap-add multiplies) + recording again ESCALATED unboundedly → Inf/NaN in the reel → blow-up/silence on playback. Fix: firewall the reel write — flush NaN/Inf→0 and hard-clamp ±1 (full scale; transparent below it, breaks the loop since the reel can never grow past ±1). Distortion emphasis states are an unprotected latent gap too, but they're stable one-poles fed sanitized input so can't blow up from finite signal — left as-is. Verified headless: hot ×3 input + delay fb 0.98 + moog res 3.8 → reel bounded (avg 0.89), output finite, \|max\|=0.9999, no NaN, non-silent. | (inline; `src/reel.c`) |
| B16 | Switching to `headless 0` silences playback/recording (input noise source, no control inlets driven) | **FIXED (2026-06-19)** | Owner repro (their proposed test): noise in → recsplice → record 0 → play → flip `headless 0` → playback dies. Reproduced (before-flip RMS 0.44, after-flip 0.00). **Bisected: NOT a recent regression** — the pre-recent-changes build (4d6fd4c) silences identically, so the recsplice/stut/bencina/reel/envelope work didn't cause it; it's longstanding headless-0 behavior. Sole cause: `amplitude` (inlet 21, grain output level) was the one engine-critical param still honoring a literal 0 in headless 0 — grainsize/iot/speed already ignore a bare 0 in both modes. Unconnected amplitude reads 0 → grain level 0 → whole engine silent (sos=0 separately drops input passthrough). Fix: amplitude now ignores an exact-0 reading in both modes (its headless-1 behavior), matching grainsize/iot/speed; a true silence is set via the `amplitude` message or a near-0 signal. Verified: after-flip RMS 0.25 (playback survives). | (inline; `src/ligase~.c`) |
| B17 | Intermittent: ligase "initializes unable to record" — splices empty after record, Moog metering/clicks on silence; needs Pd/plugdata restart | **FIXED (2026-06-19) — owner verify** | Owner: sometimes on init, recording yields empty splices and the engine runs silent (Moog shows meter blips + clicks when adjusted = filtering silence); a restart clears it. Ruled out: perform statics (debug + organize, benign), constructor (all-or-nothing null-checked), headless recording (reliable). Cause = host init-timing × a destructive reel reset: `reel_set_sample_rate` WIPED the reel (`length=0, count=0, splices cleared`) on any rate change. plugdata/JUCE (re)reports the sample rate one+ times AFTER instantiation; a rate report arriving once the user had recorded wiped the take → empty splices + dead/click-on-adjust engine until restart (clean rate negotiation). Fix (`reel.c`): preserve recorded audio (memcpy, capped to new capacity) + splice markers across a rate change instead of wiping. Verified: standalone reel test shows length/content/splices survive a 48k→44.1k change (pre-fix: wiped to 0); record+play regression clean (RMS 0.48). Couldn't repro the exact host timing headless — owner to confirm; if it persists, `play` in the bad state prints reel length / splice count / buffer level to pin empty-reel vs records-silence. | (inline; `src/reel.c`; `/tmp/treel.c`) |
| B18 | Bencina delay too subtle to be useful; inlets 11 (time) / 12 (feedback) feel like they barely do anything | **DONE (2026-06-19) — owner ear-test** | Owner: Bencina subtle, 11/12 minimal effect. Measured: 11/12 DO work (feedback tail 0.002→0.058, delay-time tail 0.0002→0.062) but on a quiet cloud. Root: wet level is dominated by grain DENSITY (overlap = grain_len/spacing) and is UNCOMPENSATED — default ~2× overlap → wet ≈ dry (subtle); dense (10×) → peak 13.8 (rails). No good operating point. Fix (`grain_delay_bencina.c`): (1) overlap-normalize the grain sum (`gnorm = 2/max(overlap,2)`) so density = TEXTURE not LEVEL; (2) `BENCINA_WET_GAIN` (1.8×) makeup on the OUTPUT only (not the feedback path → loop stays stable, loop gain = feedback<1); (3) denser default (spacing 50→25 ms ≈ 4× overlap, smoother cloud, level unchanged by normalization). Verified: wet ~1.8× louder (peak 0.26→0.47), inlets 11/12 clearer range, dense grains 13.8→0.47 (no railing, = default level), full-engine fb 0.95/mix 0.7 bounded (max 0.24, RMS 0.14, no blowup). Owner ear-test + tune `BENCINA_WET_GAIN` if needed. | (inline; `src/grain_delay_bencina.c`; `/tmp/tben.c`) |
| B19 | Smear parameters are not modulation targets; they need to be | **DONE (2026-06-19)** | Owner: smear params must be param_range modulation targets. Added `smear_frequency`/`smear_resonance`/`smear_stages`/`smear_feedback` to the modulation system: 4 new `param_range_t` fields in the scheduler struct (`types.h`) + init in `scheduler_create` (`grain.c`); per-block `sample_param_range` + apply in `ligase_perform` (grain_smear_t is opaque + sample ignores base when enabled → 0 placeholder base); entries in `get_param_range_by_name`; added to the `rand_type` 'all' list. Verified: all 4 accept `param_range`/`rand_type` (no 'unknown parameter'), and modulated freq+feedback runs bounded/non-silent (max 0.20, RMS 0.14, no blow-up). Manual modulation list updated. | (inline; `types.h`, `grain.c`, `ligase~.c`; `/tmp/t_smearmod*.pd`) |
| B20 | Stut repeated-unit length was hardwired to = stut_spacing (not grainsize, no independent control) | **DONE (2026-06-19)** | Owner: in TidalCycles stut repeats a sample; here the repeated unit's LENGTH was locked to the spacing (`play_length = spacing_samples`), tied to neither grainsize nor an independent control. Added a mode + length system mirroring the delay/IOT quantization pattern: `stut_length_mode` (0=independent, 1=grainsize); `stut_length <ms>`; `stut_length_quantize <note>` + `stut_length_quant <amount>` (BPM note-grid blend). ligase_stut computes the slice length per mode and passes it to grain_delay_stut_trigger (new `play_length_samples` arg); length is now decoupled from spacing (length<spc=gated, =spc=gapless, >spc=overlap). Verified: stut_length 30→30ms / 250→250ms slices; mode 1→100ms (grainsize); BPM 120 + quantize 8 + quant 1 → 250ms (1/8) slice. Manual + quick-list updated. | (inline; `ligase~.c`, `grain_delay_stut.c`; `/tmp/t_stutlen.pd`, `/tmp/t_stutq.pd`) |
| B21 | In headless 1, inlet 11 overrode the `stut_reps` message (only one stut regardless of the message) | **FIXED (2026-06-19)** | Owner: stut_reps message ignored / 'only one stut', and rightly noted headless 1 should not require disconnecting. The inlet-11→stut_reps code (Seq 36) did NOT gate on headless mode, so a value on inlet 11 (which doubles as the DD-4/Bencina delay-time inlet; a delay time >= 1 s → (int) >= 1) was read as the count every block, stomping the `stut_reps` message even in headless 1. Fix: drive stut_reps from inlet 11 ONLY in headless 0; in headless 1 ignore the inlet so the message/modulation is authoritative. Verified: headless 1 + sig~2 on inlet 11 + `stut_reps 8` → count 8 (message wins); headless 0 same → count 2 (inlet drives). Manual inlet-11 note updated. | (inline; `ligase~.c`) |
| B22 | `stut_reps`/`stut_reduction`/`stut_spacing` messages ignored ("trigger + stut_length work, everything else broken"); ROOT CAUSE = uninitialized param_range `.enabled` flags from `malloc` garbage → intermittent sos/delay/skew failures all session | **FIXED (2026-06-19)** | Owner: in headless 1 only `stut` + `stut_length`/`stut_length_mode` worked; reps/reduction/spacing dead. Two bugs. (1) Inlet gating: B21 gated inlet 11 for headless 1 but inlets 12/13 still drove stut_reduction/stut_spacing unconditionally, so connected delay-feedback/tone signals stomped those messages every block. Fix: inlets 12/13 now follow the same contract as inlet 11 — drive in headless 0, ignored in headless 1 (`ligase~.c`). (2) ROOT CAUSE: `scheduler_create` (`grain.c`) `malloc`s the scheduler but never initialized 6 `param_range_t` fields — `gdelay_feedback_range`, `gdelay_tone_range`, `gdelay_mix_range`, `organize_range`, `sos_range`, `env_skew_range`. Each carries an `enabled` flag the perform checks per-block to APPLY that range's modulation; a garbage-enabled range silently overwrites its parameter with garbage every block, non-deterministically per instantiation. This is the mechanism behind the whole session's intermittent reports: sos_range→"can't record/playback stops"; gdelay_mix_range→"delay just attenuates/no wet"; gdelay_feedback_range→"stut reduction stuck at 0" (reproduced here: reduction read 0.00 not 0.50); plus skew/organize glitches. Fix: `memset(sched,0,...)` immediately after malloc (defense-in-depth — guarantees enabled=0 for every range incl. future fields) + explicit `= default_range` for the 6. Verified deterministic across 5 runs each: headless 1 → messages win (reps 8 / red 0.50 / spc 62.50); headless 0 → inlets win (3 / 0.90 / 200). Trigger post now shows reps+reduction+spacing. Manual "Setting stut parameters" + inlet-11/12/13 notes updated. | (inline; `grain.c`, `ligase~.c`; `/tmp/fix{0,1}.pd`) |
| B23 | Stut cannot layer overlapping triggers — "banging the trigger creates clicks and doesn't layer" | **FIXED (2026-06-19)** | Owner: re-triggering stut clicks and does not stack; asked whether stut is even polyphonic, noting other modes show how to do this. Root cause: `grain_delay_stut_trigger` wrote grains into FIXED pool slots indexed by repeat number (`grain_pool[i]` for i in 0..reps-1), so every trigger overwrote slots [0..reps-1] — a second trigger clobbered the still-playing grains of the first (discontinuity = click; no layering). Bencina already does it right (`get_free_grain` scans for an inactive slot); stut did not. Fix: added `get_free_stut_grain` (mirrors Bencina) and rewrote the trigger to allocate each repeat into a FREE pool slot; stopped resetting `num_active_grains` in the trigger (process recomputes it) so earlier trains are not erased. Decoupled the per-train repeat cap (MAX_STUT_REPS=16) from the pool size (MAX_STUT_GRAINS 16→64) so overlapping triggers stack up to 64 voices. Verified: reps-4 trigger → peak 4 active voices; a second trigger 120 ms later → peak 7 (trains coexist; old code stayed at 4). Single-trigger reps still scales (reps2 tail silent / reps12 tail rings). Manual architecture + "Max voices" + polyphony note updated. | (inline; `grain_delay_stut.c`, `types.h`; `/tmp/slayer.pd`) |
| B24 | Stut "only the first hit plays, trailing repeats never work; reps/reduction/spacing inert" — root cause = headless-0 unconnected-inlet trap | **FIXED + DOCUMENTED (2026-06-19)** | Owner: trailing repeats never play; only stut_length/mode respond to messages. Root cause: rep 0 always plays at full gain (reduction^0=1) while trailing reps scale by reduction^i, so reduction=0 ⇒ only the first hit. In **headless 0** an UNCONNECTED inlet reads 0.0 and is honored, so inlet 12 forced stut_reduction→0 (kills trailing reps) AND inlet 14 forced gdelay_mix→0 (kills wet) every block; only inlet-less params (length/mode) survived via message — exactly the reported symptom. Confirmed headless: hl0 trigger printed `reduction 0.00` despite `stut_reduction 0.85` message; hl1 printed 0.85 and the audio showed trailing reps at 500-900ms then silence after the 1200ms span, while hl0 leaked constant dry (mix forced 0). Fixes: (1) inlet 12 now requires value STRICTLY > 0 to drive reduction (an unconnected/bare-0 inlet 12 no longer zeroes it), matching inlets 11 (reps>=1) and 13 (spacing>=1); verified hl0 AND hl1 now hold `reduction 0.85` with inlets unconnected. (2) Manual: added the "headless-0 unconnected-inlet trap" warning + steer to headless 1 for message control (gdelay_mix still follows the hl0 drive-all-inlets contract). Net guidance: use headless 1 for message-driven stut; nothing needs connecting. | (inline; `ligase~.c`; `/tmp/h{0,1}.pd`, `/tmp/saud3*.pd`) |
| B25 | Stut inlets were special-cased (ignored in headless 1) instead of using the standard epsilon convention — owner: "do it like every other inlet that works with a message AND in either headless mode" | **FIXED (2026-06-19)** | Owner runs with ~sig/sliders patched to the inlets and won't disconnect; wanted stut inlets 11/12/13 to behave identically to every other inlet (gdelay_time/feedback/tone/grainsize/...). The B21/B22 fix had special-cased stut (`if (!x->headless_mode)` = ignore the inlet entirely in headless 1), which made stut the odd one out and was the avoidable source of the whole saga. Removed the special-case; the three stut inlets now use the SAME epsilon pattern as their delay-inlet counterparts: a connected signal/slider drives the value in BOTH headless modes; headless 1 ignores a sub-floor/unconnected inlet so the message holds; headless 0 honors the literal value. Floors match each param (reps>=1, spacing>=1ms both modes; reduction mirrors gdelay_feedback: h1>=0.001, h0>=0.0). Verified: sig~(3,0.8,200) on inlets 11/12/13 → trigger prints reps 3/red 0.80/spc 200 in BOTH headless 0 and 1 (inlet drives, like any other inlet); message-only + unconnected inlets in headless 1 → reps 8/red 0.85/spc 150 (message holds). Manual "Setting stut parameters" + inlet-11 + Active-inlets rewritten to the unified convention. Net: owner's connected sliders now drive reps/reduction/spacing in either mode; messages are the fallback. | (inline; `ligase~.c`; `/tmp/drv{0,1}.pd`, `/tmp/msg1.pd`) |
| B26 | `make` produced stale/mismatched binaries — object files had NO header dependencies, so editing any header (incl. struct-layout changes in types.h) did NOT trigger recompilation; the link silently reused old objects. This is almost certainly why fixes appeared to "do nothing" across the whole stut discussion (many touched types.h) and why behavior was intermittent/inconsistent (mixed struct layouts between translation units). NOT a user build error — owner built correctly every time. | **FIXED (2026-06-19)** | Added `-MMD -MP` to CFLAGS (compiler emits a per-object .d listing every header it includes) + `DEPS = $(OBJECTS:.o=.d)` + `-include $(DEPS)` + clean removes .d. Verified: clean build emits 13 .d files; `grain.d` lists types.h; `touch src/types.h && make` now recompiles 11 objects (was 0). Stutter then confirmed working on the correctly-built binary (8 distinct bursts decaying by reduction 0.97, deterministic over 3 runs). Owner action: pull, then `make clean && make` ONCE for a clean baseline; thereafter incremental `make` rebuilds correctly on any header change. | (inline; `Makefile`) |
| B27 | Stut produced phasing/simultaneous layering instead of a stutter — overloaded inlets carried delay-ranged values that collapsed stut spacing | **FIXED (2026-06-19)** | Owner observed: normally a single grain of `length`; maxing inlets 11/12/13 gave phasing / extremely short delay / simultaneous layering, no rhythmic stutter. Diagnosed with a schedule print: inlet 13 drives stut_spacing, but inlet 13 is the delay `tone` inlet (range ~0-1) reused in Stut where spacing is 1-5000 ms — so a tone-scaled value set spacing to ~1 ms, scheduling every repeat at 0/48/96... samples (≈ simultaneous) → comb-filter phasing, not echoes. Same range clash on inlet 11 (delay_time 0-10 s vs reps 1-16). Owner's key point: every other inlet has ONE range; only the delay inlets are forced to carry 3 different ranges (one per delay mode) + the stut meanings, so no single patched slider can serve both delay and stut. Fix: stut_reps/reduction/spacing are now MESSAGE-only (like stut_length/mode); inlets 11/12/13 are inert in Stut mode and keep their single delay range. Control stut from UI via message (`stut_spacing $1`, etc.); param_range modulation still available. Verified: with sig~ 1/0.5/0.5 patched on inlets 11/12/13 AND messages reps8/spc200/red0.97, trigger renders 8 distinct bursts 200 ms apart decaying 0.97 (0.167->0.135) — inlets no longer interfere. This reverses the inlet-driven approach of B21/B22/B25 (which couldn't work given the range clash). Manual: inlet-11 ref, Active-inlets, Setting-stut-parameters, per-mode table all rewritten to message-only. | (inline; `ligase~.c`, `grain_delay_stut.c`) |
| B28 | Stut must be SIGNAL-driven (hardware prototype — physical controls/CV on the inlets), with the shared-inlet range clash solved by mapping rather than by dropping inlet control | **FIXED (2026-06-19)** | Owner clarified this is a prototype for a physical synth and the delay modes MUST be sig-driven, so B27's message-only approach was wrong. Also corrected a false claim of mine: DD-4 and Bencina use inlets 11/12/13 IDENTICALLY (delay_time 0-10 s, feedback 0-1, tone 0-1 — verified grain_delay.c vs grain_delay_bencina.c), so there are only TWO range sets (the two continuous modes share one; Stut differs), and inlet 12 (feedback 0-1) already equals stut_reduction (0-1) with no clash. Fix: inlets 11/12/13 are signal-driven in Stut and MAP their delay-native input range to the stut parameter — inlet 11 (0-10)->reps 1-16 linear; inlet 12 (0-1)->reduction 0-1 direct; inlet 13 (0-1)->spacing 1-5000 ms exponential. One physical control/CV per inlet now drives all three modes. Messages still address the params in native units (count/0-1/ms) and apply when the inlet isn't driving. Verified (trigger post): sig 10/0.8/0.5->reps16/red0.80/spc70.71; 5/0.5/1.0->reps9/red0.50/spc5000; 0/0/0 (h0)->reps1/red0/spc1; and audio: sig 4.5/0.95/0.622 -> 8 spread bursts ~200 ms apart decaying 0.95 (no phasing). Manual + per-mode table rewritten to the mapped signal-driven model. | (inline; `ligase~.c`; `/tmp/map.pd`, `/tmp/sigaud.pd`) |
| B29 | DD-4 delay-time changes zipper/click (the read tap jumps); needs a user glide parameter | **DONE (2026-06-19)** | Owner: DD-4 needs a glide param to avoid delay zipper on change. DD-4 is a single moving read tap, so a delay-time change sweeps the tap (repitch); an abrupt change = discontinuity/click. DD-4 already had a FIXED ~20 ms one-pole smoothing (hardcoded 0.001); made the rate user-controllable. Added `delay_glide <ms>` (0-5000, default 20 — preserves prior behavior) -> `grain_delay_set_glide` -> `delay->delay_glide_ms`; the dd4 process computes the one-pole coefficient from it (`1 - exp(-1/glide_samples)`, glide 0 => 1.0 = instant). Smooths BOTH message- and signal-inlet (CV on inlet 11) delay-time changes. Bencina/Stut don't need it (Bencina grains capture a fixed offset at trigger; Stut has no tap). Verified objectively: half-cycle-misaligned delay change (0.05->0.10 s @330 Hz) peak sample-to-sample jump = 8373 at glide 0 vs 388 at glide 500 / 400 at glide 50 (~21x de-zipper). Message-only (no free inlet); it governs HOW the delay time is smoothed. Manual quick-list + delay section updated. | (inline; `types.h`, `grain_delay.c`, `ligase~.c`; `/tmp/glide.pd`) |
| B30 | Bencina wet clips when driven (makeup gain too hot + no output limiter) | **FIXED (2026-06-19)** | Owner asked if Bencina is enveloped like the grain engine (clipping). Answer: yes — it shares the scheduler envelope object and applies envelope_sample per grain over normalized phase, and additionally overlap-normalizes the grain sum. So enveloping was not the cause. Real cause: BENCINA_WET_GAIN=1.8 makeup on the wet output (the grain engine has none) pushed a ~unity grain sum to ~1.8 → clipped at mix 1 even with zero feedback; feedback (buffer soft-clipped at ±4) made it worse; nothing limited the wet (ligase_sanitize_pair only flushes NaN/Inf, does not clamp levels) so it clipped at the DAC. Fix (owner chose 'moderate gain + tanh safety'): BENCINA_WET_GAIN 1.8→1.3, and the wet output is now tanhf(filtered*gain) — prominent but hard-bounded to ±1, graceful saturation instead of clipping. Feedback path left un-boosted/un-limited so loop gain stays = feedback (stable). Verified worst case (input 0.8, feedback 0.9, tone 0.7, mix 1): peak 0.89 (bounded, was clipping), RMS 0.60 (still prominent). Manual Bencina section notes the makeup+soft-limit + saturation character. | (inline; `grain_delay_bencina.c`; `/tmp/benc2.pd`) |
| B31 | Bencina didn't sound like a grain delay — it was a plain delay, not a granular cloud | **FIXED (2026-06-19)** | Owner: Bencina doesn't sound like a grain delay; asked to research Ross Bencina's actual tapped-delay-line granulator. Research (Implementing Real-Time Granular Synthesis, 2001): each grain reads the delay line with its OWN delay time and playback rate; the cloud comes from per-grain parameter variation. ligase defect: every grain was stamped with the SAME read_offset=delay_time and read write_pos-offset at fixed rate 1.0, so all simultaneous grains read the IDENTICAL delayed sample — the overlap-normalized sum was just a plain delay with envelope ripple ('moving tap locked to the write head' = a delay tap, not a grain). Fix (owner chose MINIMAL, NO pitch — pitch is deliberately smear + morphagene tape speed; pan left as-is): each grain now captures its own read position scattered behind the base delay by a random 0..one-grain-length (rate stays 1.0, no transposition). Overlapping grains read different points of the recent past -> diffuse granular cloud; scatter scales with bencina_grainsize. Verified: grain offsets now vary (e.g. 5469..8162 around delay 4800) where they were all identical before; output bounded/stable (peak 0.52, rms 0.19 at mix1/feed0.9). Note: scattered (incoherent) sum runs a bit quieter than the old coherent stack; makeup+tanh set level. Manual Bencina section rewritten (cloud via scatter, rate-1/no-pitch rationale). | (inline; `grain_delay_bencina.c`; `/tmp/benc2.pd`) |
| B32 | Bencina cloud was mono (center-panned); add per-grain random pan as a modulation target on the pan inlet | **DONE (2026-06-19)** | Owner: add random per-grain pan to the Bencina cloud (spread it in stereo, not just time), bound to the pan inlet, and make it behave like every other modulateable param — the existing modulation system's range already gives the width. Implemented `bencina_pan` exactly like the main per-grain pan: new `bencina_pan_range` param_range (types.h + scheduler_create + get_param_range_by_name + rand_type all-list + output_param_range/rand_type); each Bencina grain's pan = sample_param_range(bencina_pan_range, perlin, base) where base = x->pan (the pan inlet 22 / `pan` message). Plumbed pan_base + range ptr + perlin through grain_delay_process -> grain_delay_bencina_process -> trigger (threaded, since param_range_t/perlin_state_t are declared after the bencina struct in types.h). Range OFF -> grains at the inlet-22 base (centered); range ON -> per-grain random in [min,max] = stereo cloud, width = span, skew = position; independence via enabling the range (mod overrides inlet, standard precedence). No pitch, no separate width control (the range IS the width). Verified: range off L/R corr=1.00 (mono); `param_range bencina_pan 0 1`+rand corr=0.68 (full cloud); `0 0.35` -> LEFT-heavy (width+skew); base `pan 0.15` range-off -> LEFT-heavy (inlet base drives center). Manual + modulation-target list updated. | (inline; `types.h`, `grain.c`, `grain_delay.c`, `grain_delay_bencina.c`, `ligase~.c`; `/tmp/bpan.pd`) |
| B33 | Bencina cloud sounded clicky/buzzy/rough; but owner LIKES the grain character + uses skew-edge clickiness creatively — make smoothing OPTIONAL, keep character as default | **DONE (2026-06-19)** | Investigation: all envelope types start at ~0 (exponential is t*e^-at = 0 at onset; cosine/parabolic zero-edged; gaussian ~0.004) so there are NO hard onset discontinuities (objective slew tests: smooth output for all types/skews, dense+sparse). The 'buzzy/rough' is the position scatter: summing decorrelated grains makes the level wobble at the grain rate at ANY non-zero amount (measured CV ~0.44 at scatter 0.25 AND 1.0; 0.00 only at scatter 0). Owner decided: keep the grainy character as default, expose smoothing as opt-in knobs. Final: (1) bencina_spread <0-1> scales position scatter, DEFAULT 1.0 = full (committed character); 0 = coherent/smooth. (2) bencina_edge <0-1> raised-cosine grain edge-round, DEFAULT 0 = OFF (envelope/skew edges + their clickiness left intact); >0 de-clicks. Reverted the briefly-tried default-scatter-0 and always-on edge round. Verified defaults reproduce the committed character (roughness CV 0.389 = grainy, edges intact); bencina_spread 0 -> CV 0.000 (smooth); bencina_edge 1 -> edges rounded (still grainy). bencina_pan (B32) unchanged. Manual updated. | (inline; `types.h`, `grain_delay_bencina.c`, `ligase~.c`; `/tmp/be.pd`) |
| B34 | Bencina too quiet after the tanh + scatter changes — restore volume, keep tanh | **DONE (2026-06-19)** | Owner: Bencina too quiet, keep the tanh soft-limit but bring the level back. Cause: B30 dropped the wet makeup 1.8->1.3 (for the tanh), and B31 position scatter sums grains incoherently (quieter than a coherent stack); combined, the wet sat at ~0.11 RMS / 0.29 peak at mix 1 — the tanh wasn't even engaged (tons of headroom). Fix: since the wet is tanh-limited, drive more gain INTO it — raised BENCINA_WET_GAIN 1.3->6.0. Measured (steady tone, mix 1): RMS 0.11->0.46, peak 0.29->0.89 (clean, tanh catches transients); still bounded at feedback 0.9 (peak 0.889). Output-only (feedback path un-boosted) so loop stays stable. Comment + manual updated. (Tunable; could be exposed as a level knob if desired.) | (inline; `grain_delay_bencina.c`; `/tmp/blvl.pd`) |
| B35 | Expose Bencina wet makeup as a control (bencina_level) | **DONE (2026-06-19)** | Owner: expose the wet makeup gain as bencina_level. Added a `level` field to grain_delay_bencina_t (default BENCINA_WET_GAIN=6.0), used in place of the macro in the process (tanhf(filtered*level)), with grain_delay_bencina_set_level (clamp 0..64) + `bencina_level <gain>` message. Higher = louder/more saturated; tanh still bounds output to ±1 (can't clip). Verified: default (no msg) RMS 0.441/peak 0.902 (= the 6.0 baseline, unchanged); bencina_level 2 -> 0.184/0.444; bencina_level 12 -> 0.670/0.993 (hot, still <1). Manual quick-list + Bencina wet-level note updated. | (inline; `types.h`, `grain_delay_bencina.c`, `ligase~.c`; `/tmp/blv.pd`) |
| M1 | PDF manual regeneration — single source + `make manual` | **DONE (2026-06-16)** | All gates cleared. Source of truth = `docs/ligase_manual.md`; `make manual` regenerates `ligase_manual.pdf`; `src/ligase_manual.txt` deleted. | `Plans/pdf_manual_regeneration.md` |
| B8 | CPU saturates to 100% and stays (audio glitch, control lost) — unbounded loops/state | **FIXED — hardware-verify pending** | 3-agent hunt (playhead/delay/fog; scheduler excluded per owner). Root class: unbounded `while`-wraps + unbounded filter state in the audio thread. Fixes: (a) playhead wraps → O(1) `wrap_to_splice` (fmodf; guards zero-length splice + non-finite pos — the scanning-playhead hang); (b) delay: `buffer_size<=0` dispatcher guard + fmodf read-pos + NaN/denormal flush (incl. Bencina lpf, previously unflushed); (c) fog: phase-unwrap `while`→fmodf fold (the Inf-delta infinite loop) + wrapped/bounded `phase_prev` + clamped mag-filter STATE + isfinite firewall before the overlap-add buffer. Likely the true "stuck at 100%" cause (supersedes B6). Verified: builds, delay/TLA regression clean. Owner to confirm over a long session w/ scanning + delay + fog. | (inline; 4 files) |
| B7 | fftease won't load w/ ligase; fog broken + fog-inlet CPU spike (symbol collision) | **FIXED — hardware-verify pending** | Root cause: every non-static function was exported into Pd's flat namespace (`-undefined dynamic_lookup`), so ligase's vendored `kiss_fft`/internal symbols collided with other externals — blocking fftease and letting fog's FFT calls bind to the wrong implementation (garbage → CPU spike). Fix: `-fvisibility=hidden` + export only `ligase_tilde_setup` (`LIGASE_PUBLIC`). Verified: only `_ligase_tilde_setup` exported, 0 internal leaks; still loads/runs. Likely the real cause of the fog-inlet CPU spike (supersedes the B6 attribution). Owner to confirm: fftease loads, fog works, CPU flat. | (inline; Makefile + ligase~.c) |
| B6 | CPU creeps up over minutes (denormals on x86) | **FIXED — hardware-verify pending** | Diagnosis: subnormal floats are ~100× slower on x86 and accumulate in effect-feedback states as audio decays toward silence; the dev had added per-state flushing to delay/fog/moog but **distortion IIR states were unflushed** (and distortion runs by default), and a slow feedback decay reaches denormal range after ~minutes — matching the symptom. Fix: enable hardware FTZ+DAZ at the top of `ligase_perform` (`LIGASE_FLUSH_DENORMALS`, x86-guarded) — comprehensive across the whole chain. Verified: builds, runs, `stmxcsr`/`ldmxcsr` present. Can't reproduce the multi-minute creep headless (timescale); owner to confirm on hardware (playhead 3 + quant, run a few minutes). | (inline) |
| B9 | Fog wet path far quieter than dry ("adding fog drops output volume") | **FIXED — ear-test pending** | The makeup gain only matched NOISE; tonal/musical material still collapsed ~37 dB. Real cause: the temporal PHASE filter modified each bin's phase per frame, breaking overlap-add coherence across the 4x-overlapping frames so coherent tones destructively cancel (noise has no coherent phase to cancel — why noise looked fine). A phase-vocoder rewrite (propagate a smeared instantaneous frequency) was tried to keep the watery character, but per-bin propagation from arbitrary start phases scrambles a leaked tone's inter-bin relationships → intermittent ~20 dB collapse (~1 run in 8). Chosen fix (the "fall back to magnitude-domain blur" option): leave phase UNTOUCHED → coherent reconstruction. Fog haze is magnitude-only (frequency smear + temporal magnitude filter). Level match: the makeup-gain approach (Seq 20) only worked when fog was ramped up mid-stream — with fog ON from the start (real use) it mistracked and left wet ~26 dB down ("not noticeable until high wet %"). Replaced (Seq 21) with DETERMINISTIC per-frame spectral-energy normalization in `process_fft_frame` (rescale processed magnitudes to the input frame's energy); makeup-gain machinery removed. Verified headless (averaged): pure OLA reconstructs exactly at dry every run; with the effects on, wet sits at noise −0.7 dB, 220 Hz −1.7 dB, 440 Hz −4.0 dB (was 26–37 dB). KNOWN RESIDUAL: modifying magnitude with the original phase isn't guaranteed OLA-coherent, so certain stochastic granulated-tone realizations dip a few dB (occasional, not a dropout; pure passthrough never does). Phase processing gone ⇒ `fog_phase_cutoff` inert (manual/setter to reconcile). One report of total silence after recording into a splice — not reproduced (fog on/off both played back); owner watching. Owner ear-test: level + haze musical. Future: phase-locked vocoder for a watery character without cancellation. | (inline `grain_fog.c`/`types.h`; `test_fog_const.pd`, `test_silence.pd`) |

### SLB lane (planner; runs in parallel, agent does not wait on these)
- ✓ Manual-content-edits plan authored (`Plans/manual_content_edits.md`), seeded with Worklist A (modulation coverage audit). Awaiting the user's incoming content changes to fill stream 1.

### FRIEND lane (advisory)
- _(none)_

### AUDITOR lane (audit agent; runs the audit loop, idle-fallback only)
- _(none)_

## §2. ON DECK (planner-ordered; promote into §1 when the blocking item clears)
- **Prototyping / UI / VST arc (Seq 71-78)** — two of three shipped; statuses live in §4a:
  1. **Pd panel prototype** `Plans/pd_panel_prototype.md` — **steps 1-5 DONE + headless-verified (Seq 76/78)**: `panel_layout.py` (the surface AS DATA) + `emit_svg.py` (SVG byte-identical) + `emit_pd.py` → `pd/ligase_panel.pd`/`pd/ligase_xpndr.pd`, plus `make bundle` → `dist/ligase.plugdata` (loader-matched, deterministic, install-simulated). Remaining: **Step 6 owner hands-on** (Mac `make && make bundle`, drag-drop, feel test) — not agent work.
  2. **Scope taps** `Plans/scope_taps.md` — ✅ **DONE + headless-verified (Seq 74)**; owner feel test with `[oscilloscope~]` remains.
  3. **VST/AU plugin** `Plans/vst_plugin.md` — at GATE A. v1 = plugdata with ligase COMPILED IN (documented CMake externals-target + Setup.cpp mechanism; pinned-tag vendoring; the automated test procedure run INSIDE the built plugdata = the engine-identity gate), panel patch as the GUI, ~16 host params; v2 = native JUCE extraction (own plan when greenlit; schema-v4 text = the preset format for free). **License decision DEFERRED by owner (Seq 77)** — personal-use builds unaffected; re-raise at distribution time. B7 symbol-visibility interplay with static linking flagged (vendored kiss_fft is the known hazard).
  4. **Web build (ligase in the browser) + primase clock** `Plans/web_build.md` — NEW (Seq 91), at **GATE A**, two arcs. **A. web:** libpd→WASM (Emscripten AudioWorklet) with ligase COMPILED IN (WebPd can't run externals — the 3rd compiled-in target after plugdata-VST); GitHub Pages + Actions CI (no Mac needed); `panel_layout.py` → a 4th `emit_web.py` emitter driving the `lgR_*` bus (libpd has no GUI); audio-in via getUserMedia→adc~; reel I/O via file-input→MEMFS→load and save→MEMFS→Blob download (no native FS); single-threaded to dodge the COOP/COEP-on-Pages gotcha. **License bonus:** BSD libpd + MIT Emscripten → the web build is DISTRIBUTABLE under GPL-2 with no relicense (unlike the VST path). **B. primase~ as trigger/clock** (desktop + web): the sibling compiled external (`sbenjam1n/primase`, added to session) — outlet-1 bang/event → ligase `stut`, primase-as-master-clock → ligase cycle, position/velocity → matrix routes; NO ligase/primase engine change (bang/stut/play-1 already exist). primase packaged into the WASM build (web) + optional in `emit_bundle.py` (desktop). **primase has NO license file** = distribution blocker (owner adds one, GATE B.6).
- **Harmonic / notation arc (Seq 80-88)** — engine DONE, surface wiring in flight:
  1. **Harmonic layer** `Plans/harmonic_layer.md` — ✅ **DONE + headless-verified (Seq 85, commit 9a90eec)**: scale slots ×16 per destination (`pitch_scale_slot`, slot 0 = legacy, bit-identical defaults), `scale_root` (±24 st, quantize default ON) + `scale_rotate` as param_range/matrix destinations, stochastic scale-blend rule, `scope_tap scale`/`pattern` on outlets 10/11, capture schema v5 (v1-v4 import compat). Giant Steps demo passing. Owner ear-tests remain.
  2. **SEQ/SCALE sidecar** `Plans/seq_scale_sidecar.md` — SURFACE (depends on 1, satisfied): the tone/time-circle + slots/commit + pattern-grid block is drawn into the MAIN panel (integrated Seq 83-87; the standalone mockup `ligase_seq_panel.svg` was DELETED at Seq 83 when it folded in). **Wiring IN PROGRESS** (Seq 88): folding the silkscreen into `panel_layout.py` SEQ_* records + `emit_pd.py` wiring against the harmonic engine (ring→scale list, ROOT/MODE→root/rotate, AXIS→SLOTS Coltrane generator, Euclid K/N/ROT, pattern grid, cold-edit+APPLY). TONE CIRCLE 12-pin ring, RING ORDER lenses, Messiaen polygon presets, SLOTS/COMMIT with the 16 A-P bank + SENDS readouts, XPNDR-addressed grid.
- **New directions (6 plans, owner-requested exploration)** — ✅ **ALL SIX IMPLEMENTED** (Morph at Seq 59–60; the remaining five at Seq 61, each on its plan's recommended [R] GATE-A options). Every v1 headless-verified + automated-test-procedure regression clean; owner ear-tests remain (chord balance, spatial orbit feel, resonator timbre = the GATE A.7 KS input). Original priority order:
  1. **Morph / Metasurface** `Plans/morph_metasurface.md` — snapshot interpolation across a 2D surface + coordinate cursor + route automation (Ross Bencina's Metasurface; research confirmed Natural-Neighbour/Sibson is the true kernel — v1 ships an IDW/Shepard kernel behind a kernel-agnostic API, NN deferred to v1.x; routes with per-leg rate+curve are the value-add over AudioMulch).
  2. **Polyphony** `Plans/polyphony.md` — chordal granular (Reading A: a voice pool + N-transposition grain spawning over the ONE shared playhead; Reading B = parallel `ligase~` instances, out of scope). Playhead/splice/SOS untouched; the live decision is the grain-pool budget.
  3. **Spatial granulation** `Plans/spatial_granulation.md` — unflatten the nbody/sphere 3D trajectories to drive per-grain placement (v1 stereo/binaural; multichannel >2 outlets = a class-construction change, separate v2).
  4. **Modulation matrix** `Plans/modulation_matrix.md` — N→M routing (connections + depth, summed) + input-listening sources (envelope follower v1; onset/pitch v2). Additive over the 1:1 param_range system.
  5. **Event-firing patterns** `Plans/pattern_events.md` — a pattern step fires grain-burst/splice-select/retrig/gate/bang + Euclid `(k,n)` + `rev`; reuses the cycle clock + step table.
  6. **Resonator bank** `Plans/resonator_bank.md` — distinct mode: a bank of N tuned resonators (reuse the smear allpass) excited by the granular bus = exciter→body; Karplus-Strong weighed as v2 after ear-test.
  All six researched (web + codebase) and adversarially verified; each stages a scoped v1 + deferred extensions.
- **Smear pitch + dual-destination MIDI + fine-tune (pitch-destination arc)** — **P1+P2+P3 ALL IMPLEMENTED + verified (Seq 52/53/54). Pitch-destination arc COMPLETE.** Three plans, dependency chain **P1 → P2**; **P3** is an independent grain-half + P1-dependent smear-half rider. Architecture: two independent pitch DESTINATIONS — grain (exists → playback speed) and **smear/resonator** (new → `smear_frequency` Hz) — each with a selectable source (off/semitone/scale/MIDI/pattern) **and a modulatable ±50-cent fine tune**. **P1** `Plans/smear_pitch.md` — dedicated `smear_pitch_control_t` (not `pitch_control_t`), note→Hz (`ref·2^((note−ref)/12)`) once-per-block in the smear stanza (OVERRIDE precedence), `pattern smear_pitch` on reserved slot 6. **P2** `Plans/midi_channel_routing.md` — channel-carrying `midi <note> [vel] [channel]` + channel→destination map (same=unison, different=separate); depends on P1. **P3** `Plans/pitch_fine_tune.md` — Moog-style ±50-cent fine offset per destination, a `param_range` target (`pitch_fine`/`smear_pitch_fine`, per-grain/per-block); grain half independent, smear half rides P1. **Owner GATE-A decisions (Seq 51):** A440 note→Hz ref; P1 = full source menu incl. stochastic SCALE (de-static `sample_scale_semitones`); channels independent by default (grain 1/smear 2); smear precedence = **override, consistent with grain** (a pitch source bypasses the continuous modulation, as `PITCH_MODE != OFF` bypasses `speed_range`) — **no "combine" mode** (modulatable detune/vibrato is the P3 fine-tune, not a repurposed `smear_frequency_range`). Backward compat load-bearing. Tracked in §4a.
- **One-shot playback (stop-at-splice-end)** — NEW (Seq 50). Plan `Plans/oneshot_playback.md`. **GATE A owner-approved** (nav-wins / STATIC no-op / `loop`+`trigger` / clear both flags / field on `splice_behavior_t`) — cleared to Step 1. A composable `loop <0|1>` flag (default 1=loop, 0=oneshot) that flips the existing SCANNING/CLOCK_ADVANCE `wrapped` branches from fold-and-loop to stop (`is_triggering=0`), plus a `trigger` re-arm selector (`bang` is the BPM clock, can't be reused). **Key finding: the "grains finish on their own" requirement is already met** — the render block (`scheduler_process`) runs unconditionally, never gated on `is_triggering`, so stopping the transport never cuts active grains. Small surface: a struct field + two perform-site one-liners + a clock-coherence gate in `ligase_bang` + two selectors. GATE-A decisions: field home, `pending_splice` precedence (nav-wins recommended), STATIC no-op, selector names, clear `is_playing` on stop. Tracked in §4a.
- **Pattern subsystem (TidalCycles mini-notation + `[4/4 3/8]` quantization cycle)** — ✅ **FEATURE-COMPLETE + headless-verified (P1+P2+P3, Seq 47/48/49).** All three plans implemented, every gate warning-free, no regression. **Remaining: owner ear-test in plugdata** (musical feel of the patterns/quantization/pitch on the Focusrite) — not agent work. Syntax is canonical Tidal mini-notation (space-separated sequences; `,`=stack is not applicable and intentionally unsupported):
  - **P1** `Plans/pattern_notation.md` — the notation parser, recursive nesting, and the free-running BPM-locked cycle clock (a *fifth* grid built from the existing `(60000/bpm)*4` math; `pattern` / `pattern_cycle` / `pattern_clear` selectors; comma-free wire syntax that survives Pd binbuf). Foundation; attaches to nothing on its own.
  - **P2** `Plans/pattern_modulation.md` — `RAND_TYPE_PATTERN` as a modulation source on any `get_param_range_by_name` target (reuses the invert/map/slew tail verbatim). Depends on P1.
  - **P3** `Plans/pattern_pitch.md` — `PITCH_MODE_PATTERN` scale-degree stepper reusing `scale.semitones[] → semitones_to_speed`, adding the wrap+octave the scale code currently lacks. Depends on P1+P2.
  - GATE-A decisions are **owner-approved (Seq 46, recommended options)**: `PATTERN_SLOTS=8` (~14 KB/instance), the `[..]`-segment vs `<..>`-value contract, strict denominator set, reject-not-truncate caps, `enabled`-on-attach, pitch dedicates the last slot. Per-plan detail in §4a / each plan's GATE A.
- **Manual content edits** — plan: `Plans/manual_content_edits.md`. M1's GATE 3 is cleared (master + `make manual` exist), so this is ready to promote on the user's go-ahead. **Worklist A is seeded** from the 2026-06-16 modulation-coverage scan (5 missing targets, `param_invert` undocumented, phantom `modout_*` messages, stale "21" count); stream 1 (the user's incoming content changes) is still to be specified.

## §3. BLOCKED (visible but skipped; ordered by what unblocks them)
- _(none.)_ M1's GATE 0 cleared (pandoc + weasyprint installed); it now rests at GATE 4, a decision checkpoint, not a block.

## §4. BACKLOG / IDLE (NOT active — pointers only; planner promotes to §2/§1)
- **Build-naming / stale-artifact cleanup:** `Makefile` still carries `erosion` naming (`@region:erosion_pd.utils`, `erosion_query_test`); `src/` has leftover `erosion~.o` and `.1` backup dupes (`kiss_fft.c.1`, `_kiss_fft_guts.h.1`). Cosmetic; promote only if a release is being cut.

## §4a. PLAN COVERAGE — queue-wide build-out (planner workstream)

| Item | Plan | Status |
|------|------|--------|
| B1 Sample-rate / buffering | `Plans/completed/sample_rate_buffering.md` | ✓ (archived 2026-06-22) |
| B2 Reel load/save on macOS | `Plans/completed/reel_io_macos.md` | ✓ (archived 2026-06-22) |
| M1 PDF manual regeneration | `Plans/completed/pdf_manual_regeneration.md` | ✓ (archived 2026-06-22) |
| Manual content edits | `Plans/manual_content_edits.md` | ◐ Worklist A + engine/record-mode updates done; deep SOS prose optional (still active) |
| Pattern P1 — notation parser + cycle clock | `Plans/pattern_notation.md` | ✅ DONE + headless-verified (Seq 47); 8/8 AC pass, warning-free, no regression |
| Pattern P2 — modulation source (`RAND_TYPE_PATTERN`) | `Plans/pattern_modulation.md` | ✅ DONE + headless-verified (Seq 48); attach/clear/slot-alloc all pass, warning-free, no regression |
| Pattern P3 — pitch via scale (`PITCH_MODE_PATTERN`) | `Plans/pattern_pitch.md` | ✅ DONE + headless-verified (Seq 49); wrap+octave/alt/nested/mode all pass, warning-free, no regression |
| One-shot playback (stop-at-splice-end) | `Plans/oneshot_playback.md` | ✅ DONE + headless-verified (Seq 55); `loop`/`trigger`, stop-at-end, nav-wins; warning-free, no regression |
| Morph / Metasurface | `Plans/morph_metasurface.md` | ● v1+v1.1 DONE (Seq 59–60) — capture/blend/surface/routes + IDW & natural-neighbour kernels + CV cursor + selection tree + 3 persistence routes (binary/text/message) |
| Polyphony / chordal granular | `Plans/polyphony.md` | ✅ DONE + headless-verified (Seq 61); voice pool (MAX_VOICES=8, steal-oldest), `poly`/`chord`/vel-0 note-off; mono path bit-identical |
| Spatial granulation | `Plans/spatial_granulation.md` | ✅ DONE + headless-verified (Seq 61); pan_mode 2 + `spatial` msg, front-biased azimuth, width 0=center/1=spread; pan_mode 1 md5-identical to pre-change binary |
| Modulation matrix | `Plans/modulation_matrix.md` | ✅ DONE incl. **v1.5** (Seq 61+62); 44 sources -> 26 dests (20 per-block + 6 per-grain at trigger); capture transparency (mod_base); contract in `docs/modulation_layers.md` |
| Snapshot expander (edit buffer) | `Plans/snapshot_expander.md` | ✅ DONE + headless-verified incl. v1.1 audition/compare (Seq 67-68); snapbuf_* edit buffer, shared 150-entry field walker, schema v3 (sources captured; v1/v2 import compat), recall/blend/apply unified on the selection tree; cold-edit byte-identity proven; snapbuf_audition/compare = exact-revert opt-in |
| Event-firing patterns | `Plans/pattern_events.md` | ✅ DONE + headless-verified (Seq 61); `pattern event grain/splice/retrig/gate/bang` + Euclid `(k,n)` + `rev`; shared 0-5 slot pool |
| Resonator bank | `Plans/resonator_bank.md` | ✅ DONE + headless-verified (Seq 61); smear_mode 1 allpass bank (16 voices, scale-tuned) reusing grain_smear verbatim; tuned peaks +28-36 dB; timbre ear-test = owner (GATE A.7 KS input) |
| Smear pitch destination (P1) | `Plans/smear_pitch.md` | ✅ DONE + headless-verified (Seq 52); note→Hz (440/880/220), pattern slot 6, override, SCALE; warning-free, no regression |
| Channel-aware MIDI routing (P2) | `Plans/midi_channel_routing.md` | ✅ DONE + headless-verified (Seq 54); unison/separate/drop/reject; warning-free, no regression |
| Pitch fine-tune ±50¢ modulatable (P3) | `Plans/pitch_fine_tune.md` | ✅ DONE + headless-verified (Seq 53); grain+smear fine, modulatable; warning-free, no regression |
| Source shapes (generator shape params) | `Plans/source_shapes.md` | ✅ DONE + headless-verified (Seq 70, commit fe90c79); waveform phase/PW/skew, lorenz σρβ, sphere spin/kick_rand; schema v4 |
| Pd panel prototype (working control surface) | `Plans/pd_panel_prototype.md` | ◐ IN PROGRESS (Seq 78); **steps 1-5 DONE + headless-verified** — panel_layout.py (layout AS DATA) + emit_svg (SVG byte-identical) + emit_pd → `pd/ligase_panel.pd`/`pd/ligase_xpndr.pd` + `make bundle` → `dist/ligase.plugdata` (loader-matched, deterministic, install-simulated); remaining: Step 6 owner hands-on (drag-drop + feel test) |
| VST/AU plugin (staged) | `Plans/vst_plugin.md` | ○ PLANNED (Seq 77); GATE A — v1 plugdata-compiled-in + panel GUI; **LICENSE decision DEFERRED by owner** (personal-use fine; decide before distribution); v2 native = own plan later |
| Web build (browser via GitHub Pages/CI) + primase clock | `Plans/web_build.md` | ✅ BOTH ARCS DONE + verified (Seq 93/94) — Arc A libpd→WASM + emit_web GUI + Actions→Pages (identity RMS 0.372309); Arc B primase~ master-clock/stut pairing (desktop + WASM, identity unperturbed). Owner gates: enable Pages, primase license (B6) |
| Harmonic layer (scale slots/root/rotate + scale morph rule + scale/pattern scope taps) | `Plans/harmonic_layer.md` | ✅ DONE + headless-verified (Seq 85, commit 9a90eec); schema v5; owner ear-tests remain |
| SEQ/SCALE sidecar (tone circle + time circle + pattern grid) | `Plans/seq_scale_sidecar.md` | ✅ DONE + headless-verified (Seq 89, commit 2b004b8); wired to the harmonic engine, SVG byte-identical, ring/AXIS/euclid/grid gates re-verified; owner plugdata feel-test + 2 B-item seams (Euclid ROT token, vexpr per-slot transpose) remain |
| Scope taps (mod/grain visualization outs) | `Plans/scope_taps.md` | ✅ DONE + headless-verified (Seq 74); scope_x~/scope_y~ outs 10/11, 11-family tap table, grain constellation + grainsum; every tap shape measured in captured data |
| Build-naming cleanup | _(none; backlog stub)_ | — |

## §5. HOW THE AGENT USES THIS (read-only protocol)

1. Read §1. Take the **top item in the AGENT lane whose Status is ACTIVE and is not BLOCKED**.
2. Do it to its gate/checkpoint. Record the outcome in a commit/PR note (and a `MESSAGE.md` note if a handoff/nuance is needed). Apply the usual discipline (verify, don't rubber-stamp; be honest about what's done vs asserted).
3. At a GATE, **STOP and report** — do not roll past a gate without the planner.
4. If the top item is blocked or you believe the order is wrong, **PROPOSE in `MESSAGE.md`; do not edit this file.** The planner reorders.
5. When you finish an item, say so in the note; the planner marks it DONE here and advances the queue (increments Queue Seq).
6. `MESSAGE.md` (SLB direction) **overrides** the planned next step for the current turn.

## §6. Version History (planner-only; sign "SLB")

| Date | Seq | Change | By |
|------|-----|--------|----|
| 2026-07-17 | 95 | **Web GUI render bug FIXED — deployed page was a gray canvas of overlapping controls** (owner: "github page doesn't render properly, gray canvas, non-functional overlapping sliders and buttons, no style or interface"). Root cause: `emit_web.py`'s GUI carried a dual-mode layout (absolute SVG-coordinate positioning vs responsive flow) and `index.html` chose absolute above 1600 px — but 144 of 304 controls (the f-string-generated SEQ pattern grid + slots, `svg=None`) had NO panel coordinates, so on any wide desktop they all piled at (0,0), overlapping into an unstyled heap. The Seq-93 headless check missed it (tested only at 1400 px, the flow branch). Fix: **retired absolute mode entirely** — `index.html` forces `const flow = true`; `emit_web.py` rewritten to a flow-only **sectioned-card** layout: new `parse_sections()` reads panel_layout.py's own `# ---- TITLE ----` markers to group each control under its real section, `_generated_section()` routes the f-string-generated ids (snap→PRESETS/SNAPSHOTS, seq_ring→SEQ/SCALE, seq_slot→SLOTS/COMMIT, seq_grid→PATTERN GRID) so ZERO controls fall through to a generic bucket; CSS reworked to `.lg-panel` flex column of bordered `.lg-sec` cards with headers, each a wrapping `.lg-sec-b` of `.lg-ctl` widgets. Verified headless at 1920/1280/800 px: 304 controls, 18 sections, **0 overlaps**, `display:flex`, no horizontal overflow, screenshot shows a clean styled Synthi-style surface. Engine wiring (lgR_/lgS_ bus, watch/setFloat/sendBang, sendDefaults) untouched; no engine/src change; identity gate unaffected (GUI-only). Owner still owns enabling GitHub Pages for the live deploy. | SLB |
| 2026-07-06 | 94 | **Web build Arc B COMPLETE + independently verified** (`Plans/web_build.md` Arc B → DONE; commit bdffbd9; both arcs now done). primase~ (the sibling external, separate repo `/workspace/primase`, NOT vendored) drives ligase as a rhythm brain, no engine change to either side. `pd/ligase_primase.pd`: `[primase 120]` master clock → `[ligase~]` — outlet-0 bang → `stut` + ligase's main-inlet cycle clock; outlet-1 position → grainstart; outlet-2 velocity → amplitude; one-shot `play 1` noted. Desktop: `emit_bundle.py` includes primase.pd_{linux,darwin} + the demo patch present-if-built (determinism + verify preserved). Web: `build_wasm.sh` compiles primase's C sources in from `PRIMASE_DIR` (TARGET=primase, `primase_setup` under `-DWITH_PRIMASE`; a build-side `-Wno-error=incompatible-function-pointer-types` handles primase.c's freemethod cast — primase source untouched). `tests/primase/` self-asserting pairing added to the suite. ORCHESTRATOR RE-VERIFIED: acceptance suite 15/15 (primase_pairing PASS: 8 events → 8 stut, 6 clock-locked at the 500 ms/120-BPM grid ligase derived FROM primase's bang interval); demo patch loads clean + drives ligase stut live; **WASM engine-identity gate STILL RMS 0.372309 / buffer 0.330109 with primase compiled in** (primase does not perturb ligase); both externals instantiate in one WASM module; primase repo unmodified (NO license added — B6 distribution blocker stands, owner's call); ligase src/QUEUE/RESUME untouched. B-items: B4 panel CLOCK-SRC switch deferred (would touch the SVG-identity gate — own task); a clean `(t_method)` cast in primase.c (owner's repo) would drop the build-side flag; primase LICENSE required before any distributed bundle/web build. | SLB |
| 2026-07-06 | 93 | **Web build Arc A COMPLETE + independently verified** (`Plans/web_build.md` Arc A → DONE; commit 13d76be). Step 2 — **engine identity in WASM**: `host_identity.c` + `check_identity.mjs` drive test_auto/test_playback inside the WASM build (libpd scheduler, reel read from MEMFS) → **RMS 0.372309 / frames 132288 / buffer L=R=0.330109**, the exact native baseline, as the required CI gate. Honest characterized delta: the saved reel is not byte-identical (32.8% of samples ≤1 ULP off; peak bit-identical; RMS/frames/buffer exact) = a cross-target x86-64/glibc↔wasm32/musl last-bit float difference (ruled out FMA, pd version, noise~) — perceptually null, NO src change. Step 3 — single-thread **AudioWorklet** (WASM engine in AudioWorkletGlobalScope; no SharedArrayBuffer/COOP/COEP → plain-Pages-safe) + `index.html` player. Step 4 — **`docs/ui/emit_web.py` = the FOURTH emitter** (289 web controls driving the engine over the existing `lgR_*` bus; readback confirmed), getUserMedia audio-in (opt-in) → `[adc~]`, reel import (file→MEMFS→load) + export (save→MEMFS→Blob); host finding (no engine change): MEMFS reel paths use absolute `/tmp/`. Step 5 — **`.github/workflows/web-deploy.yml`**: emsdk 6.0.3 + libpd static + TARGET=worklet, the identity gate as a required check, emit_web + assemble, `actions/deploy-pages` (static-only, `.nojekyll`, no headers). ORCHESTRATOR RE-VERIFIED: identity RMS 0.372309 reproduced on own run; headless-chromium boots the page on Start and injects all 289 controls (308 interactive elements); worklet module builds; CI YAML valid; src/QUEUE/RESUME untouched; build outputs gitignored. Single-threaded confirmed sufficient (no WASM_WORKERS). First real Pages deploy = owner (Settings → Pages → GitHub Actions). Arc B (primase clock, desktop+web) queued next. | SLB |
| 2026-07-06 | 92 | **Web build Arc A BEGUN — foundation proven headless** (owner: "take the recommendations, begin arc A"; GATE A cleared at all [R] for both arcs). Stood up the toolchain IN THIS ENV: Emscripten 6.0.3 (emsdk) + libpd (+ pure-data submodule) built to a static WASM archive via emcmake. **Proofs (node, no browser):** (1) stock libpd→WASM runs — `test_sine.pd` → RMS 0.141421 / MAX 0.200000 (exact 0.2-amp sine); (2) **ligase compiled in** — all 15 `src/*.c` compile under emcc, `[ligase~]` instantiates (no "couldn't create") and processes audio in WASM (the B7 kiss_fft static-link hazard is MOOT — fog/FFT was removed). A7 license audit confirmed: libpd + pure-data = BSD "Standard Improved BSD", Emscripten = MIT/NCSA → GPL-2 web build distributable with NO relicense (the strategic win over the GPLv3 VST path). Committed `web/` foundation: `build_wasm.sh` (reproducible: libpd static + ligase objects → WASM, node|worklet targets), `host_headless.c` (node engine-identity harness), test patches, README; build outputs gitignored. Remaining Arc A (delegated next): rigorous exact-baseline (0.372309) engine-identity test in WASM, browser AudioWorklet + HTML host, `emit_web.py` GUI from panel_layout driving `lgR_*`, getUserMedia audio-in, MEMFS reel import + Blob export, GitHub Actions → Pages. Arc B (primase clock) follows. | SLB |
| 2026-07-06 | 91 | **Web-build + primase-clock plan authored at GATE A** (owner: "could this be deployed online through github.io with CI? how would it handle audio in, reel import/export? could sbenjam1n/primase be used as an optional trigger/bang source for play and stut?" → "yes, add sbenjam1n/primase to the session, create the plan"). `sbenjam1n/primase` added to the session + cloned (renamed mid-turn telomere→**primase~**; HEAD 5c1cafa) — a compiled-C Pd pattern-replicator: `[primase 120]`, **outlet-1 bang per event**, out-2 position, out-3 velocity, external-clock inlet, Euclid + transform chain, OSC. `Plans/web_build.md` authored, 2 arcs. **A (web):** research-grounded — WebPd CANNOT run compiled externals, so the path is libpd→WASM (Emscripten AudioWorklet) with ligase compiled in (3rd "compiled-in" target after plugdata-VST); GitHub Pages + Actions CI (no Mac); a 4th `emit_web.py` emitter from panel_layout.py driving the `lgR_*` bus (libpd has no GUI); getUserMedia→adc~ audio-in; reel I/O via file→MEMFS→load + save→MEMFS→Blob download; single-thread to avoid the COOP/COEP-unsettable-on-Pages gotcha; **license-CLEAN** (BSD libpd + MIT Emscripten → GPL-2 web build distributable with NO relicense, unlike the VST GPLv3 conflict). **B (primase clock, desktop+web):** primase outlet-1 bang → ligase `stut`, primase-as-master-clock → ligase cycle, position/velocity → matrix routes; NO ligase/primase engine change (main-inlet bang = clock, `stut` bang-triggerable, `play 1` already exist); primase compiled into the WASM build + optional in emit_bundle.py; **primase has NO license file = the one distribution blocker** (owner adds one at GATE B.6). No code yet — awaits owner GATE-A decisions. | SLB |
| 2026-07-06 | 90 | **Acceptance-test backfill: the 5 narrative-only features now have committed re-runnable suites** (commit 80ac812; closes the Seq-88 review's "5 of 9 features narrative-only" finding). `tests/{polyphony,expander,source_shapes,scope_taps,harmonic}/` — 22 patches + per-feature READMEs + `tests/run_acceptance.sh` (14 self-asserting checks, nonzero exit on any fail), matching the 4 features that already shipped patches (spatial/modmatrix/pattern_events/resonator). Self-asserting in-patch (route/expr/sel → print PASS|FAIL) where the property is a readable value; WAV + documented sox/python metric for genuine audio shapes. Orchestrator-verified: ran the suite (14/14), NEGATIVE CONTROL confirmed asserts can fail (breaking an expected value flips PASS→FAIL — not vacuous), poly_release asserts the released transposition ABSENT, schema-v5 md5 guarded on load actually firing. Regression exact; `src/` untouched. Two honest live-vs-narrative deltas documented (not papered over): emptied poly pool plays one untransposed mono-fallback grain (release asserts the transposition disappearing, not silence); lorenz rho chaos-ratio is soak-length dependent (~2.2× in 2s vs the ~3800× long-soak figure) so the test asserts the qualitative range collapse. | SLB |
| 2026-07-06 | 89 | **SEQ/SCALE sidecar wiring DONE + headless-verified** (`Plans/seq_scale_sidecar.md` → DONE; commit 2b004b8). The last silkscreen-forward section is now live: the SEQ/SCALE block folded into `panel_layout.py` SEQ_* records (SVG **byte-identical** — geometry frozen) + `emit_pd.py build_seq_body` emitting the `[pd seq]` canvas (embedded in `ligase_panel.pd` + standalone `pd/ligase_seq.pd` over the lg_engine/lg_state9 buses; joins the `.plugdata` bundle). Wired to the harmonic engine, verified through `lgR_seq_*` → snapbuf readback: tone-circle ring composes the clicked pitch-class set (major `0 2 4 5 7 9 11`, pentatonic `0 2 4 7 9`), ROOT→scale_root / MODE→scale_rotate, DEST-routed, cold-edit + APPLY; AXIS→SLOTS arms the Giant Steps cycle (`pattern scale_root [ 0 4 8 ]`); Euclid K/N→`(k,n)` TARGET-routed; grid pins→`pattern <field> [ … ]` at the VALUE level via XPNDR addressing. **Orchestrator review caught a composition defect** the agent's first "PASS" had masked — the complementary-spigot cascade's pass-through gates only initialized on pin-toggle, so clicking only the ON pins dead-ended to an EMPTY scale (reproduced 3 ways); sent back, fixed (load-time gate-init + ring PD default all-OFF, SVG unaffected), and independently re-verified with realistic only-ON-pins drive on fresh loads (not order-dependent). Regression exact; `src/` untouched; all patches load zero-error. Two as-built engine-gap seams tracked as B-item candidates: Euclid ROT knob has no engine rotation token (visual-only); AXIS→SLOTS uses scale_root sequencing rather than per-slot `pitch_scale_to` transposition because `[vexpr]` is absent from vanilla Pd. Also refreshed RESUME.md to current (was stale at Seq 79) per the Seq-88 review's recommendation. | SLB |
| 2026-07-06 | 88 | **Queue true-up after a multi-agent QUEUE review** (owner: "think about what we have on QUEUE.md"; ultracode — 16-agent review: 5 slice-readers → synthesis → 10 adversarial claim-verifications, all held). Fixed three verified doc-vs-git discrepancies the review surfaced: (a) §4a harmonic-layer row said "build not started" — corrected to DONE + headless-verified (Seq 85, 9a90eec); (b) §4a sidecar row cited the deleted mockup `ligase_seq_panel.svg` as committed (removed at Seq 83) — corrected to silkscreen-integrated + wiring-in-progress; (c) §2 ON-DECK harmonic/notation block presented the shipped engine as pending — collapsed to a DONE pointer with only the sidecar wiring in flight. NB the panel-alignment commit 823595c (ADDRESS/SLOT-TARGET alignment) had no §6 entry — this is its record. Review's other findings (all TRUE, tracked here for the post-sidecar pass): RESUME.md stale at Seq 79 (omits the harmonic layer + wide chassis) → refresh owed; 5 of 9 engine features have narrative-only (not committed-patch) verification → backfill candidate; local `main` ref stale at 2b1103e behind origin/main 73291fa → use origin/main for diffs; VST v1 blocked only on owner GATE-A greenlight (license deferred = personal-use unblocked); kiss_fft static-link hazard is moot (no longer compiled). No code change. | SLB |
| 2026-07-06 | 87 | **Right region swapped: SEQ/SCALE on top, expander below, MONITOR back in the right corner** (owner: "swap seq scale with sections above it, putting monitor back in the right corner"). SEQ/SCALE block moved up (strips y92, beds 110–326, grid 358–562); expander paired rows moved down (SNAPSHOT|VALUE y586, ADDRESS|MOD BAND y714, COMMIT/AUDITION|MONITOR y906) so the COMMIT|MONITOR row is the bottom row and MONITOR sits in the panel's bottom-right corner. Region titles swapped to match (SEQ/SCALE top-left, SNAPSHOT EXPANDER top-right). H stays 1096, nothing past the presets line. pd patches regenerated; both load zero-error; engine untouched. | SLB |
| 2026-07-06 | 86 | **Panel goes WIDE — paired expander + SEQ/SCALE at the approved width, nothing below the presets line** (owner: "value and mod to the right of snapshot and address, monitor to the right commit audition. this should be roughly the width of the seq/scale section I originally signed off on… nothing should be pushing the length below presets"). W 1914→2456, **H back to 1096**. The right region (1508–2404, ≈ the approved 880) is now: Row 1 [SNAPSHOT | VALUE], Row 2 [ADDRESS | MODULATION BAND], Row 3 [COMMIT/AUDITION | MONITOR], then the SEQ/SCALE block below at full region width — beds OUTSIDE on the rails (216 module), control columns INSIDE beside them (POLYGON PRESET + SLOT become detented small knobs to fit the columns — the dist_preset idiom), SLOTS/COMMIT 4×4 A–P on the mirror line, PATTERN GRID full-region-width below with the XPNDR addressing — everything ends at 1034, above the footer line, level with the presets row. Seq-84's tall layout replaced wholesale. pd patches regenerated; both load zero-error. | SLB |
| 2026-07-06 | 85 | **Harmonic layer IMPLEMENTED + headless-verified** (`Plans/harmonic_layer.md` → DONE; all five step gates measured). Scale slots ×16 per destination (slot 0 = legacy; defaults BIT-IDENTICAL: md5 a04e437c… HEAD vs new; slot switch click-free), `scale_root` ±24 quantized-default + `scale_rotate` as param_range/matrix destinations (lorenz→root wander: 24/24 integer semitones across 9 roots, all in-key; `pattern pitch_scale_slot` steps at exact cycle thirds; LFO slot sweep shows no base integration; SNAP under an active pin captures the base — transparency held), D4 scale-blend = stochastic source-pick (disjoint-scale blends yield ONLY member pitches, grain per-trigger + smear per-block; pre-existing behavior was argmax hard-switch — now defined + documented in modulation_layers.md), `scope_tap scale` (79 360/79 360 samples exactly on the unit circle at the root-spun classes) + `scope_tap pattern` (plateau silhouette exact) on outlets 10/11 — no new outlets. Schema v5: walker 218 fields (91 scalars/48 discretes/32 MF_SCALE_SLOT, empty slots count-only), v1–v4 import compat (no exclude remap needed — included[] layout unchanged), text re-export byte-stable, binary v4 (+1.06 MB ≈ estimate; v3 refused with re-export path). Giant Steps acceptance: per-slot pitch histograms ⊆ the three transposed triads, alternating at the cycle rate. As-built: pattern_debug traces SCALE mode (measurement aid); new bands not snapshot state; D4 covers PATTERN modes; bank follows root not rotate, blends keep bank on argmax scale (flicker avoidance); harmonic ranges default [0,1] for 1:1 pattern indices. Regression exact everywhere; warning-free; independently re-verified (fresh clean build, regression, new-scratch slot/root/rotate/morph round-trip: save→load→save md5-identical). Manual + modulation contract updated. Remaining: owner ear tests. | SLB |
| 2026-07-06 | 84 | **SEQ/SCALE arrangement RESTORED per owner correction** ("no, stop, you destroyed the seq scale arrangement. value moves to the left of snapshot. seq scale section below that." — placement confirmed via question: right block under the expander). The Seq-83 flattened band was wrong: the APPROVED arrangement (TONE | SLOTS/COMMIT | TIME circles row, PATTERN GRID full-width BELOW) is back, as a right-aligned block under the expander column (x 1006–1862, y 1052–1745; right edge on the expander rail; H 1464→1820; bottom-left stays open). EXPANDER: VALUE moves LEFT of SNAPSHOT in one shared top row (LED+knob each half; LOAD/FROM LIVE side-by-side under the SNAPSHOT half), shortening the column — ADDRESS 220, MOD BAND 362, COMMIT 554, MONITOR 678–806. pd patches regenerated, both load zero-error; engine untouched by the panel change. | SLB |
| 2026-07-06 | 83 | **THE FINAL CHASSIS — SEQ/SCALE integrated into the main panel** (owner: "align items in expander section, seq/scale can go below these. monitor section can be condensed. hopefully this is the final panel change."). Panel H 1096→1464; the SEQ/SCALE band spans full width below every column (tone circle bed at the LX rail, twin 216×216 beds = the joystick/scope module, SLOTS/COMMIT column between the circles, pattern grid in the matrix idiom with XPNDR addressing); silkscreen-forward until `Plans/seq_scale_sidecar.md` builds the wiring (GATE A.1 REVISED: integrated band + [pd seq] subpatch + standalone canvas, XPNDR precedent; standalone mockup files deleted). EXPANDER column aligned to rails (content left 1556 / switch centers 1697 / right rail 1862: LOAD+FROM LIVE equal-width right-aligned, band knobs uniform 68px pitch, SOURCE/INST rail-fitted, commit buttons evenly distributed) with uniform 24px section gaps. MONITOR condensed 268→128 (horizontal VU pair + inline MASTER/BANK-MIX + 2-line legend incl. outs 10/11). pd patches regenerated (GUI coords follow layout; both load headless zero-error; engine untouched — regression stands). | SLB |
| 2026-07-06 | 82 | **Grid aligned + harmonic-layer BUILD STARTED** (owner: "align pattern grid left with left of tone circle, then begin"). Mockup: the PATTERN GRID bed's left edge now = the TONE CIRCLE bed's left edge (x 48; row labels in the margin, matrix idiom; addressing zone re-centered). Build: `Plans/harmonic_layer.md` Steps 1-5 handed to the implementation agent (scale slots ×16 + root/rotate destinations + stochastic scale-blend rule + scale/pattern scope taps + schema v5), gates as planned; sidecar build follows after review. | SLB |
| 2026-07-06 | 81 | **Sidecar mockup revised at owner direction** ("wasted space horizontally, condense somewhat. could you add 12 scale slots, 2 rows below current"). Chassis 1040→880 (gutters 124→60, beds at 48/616, addressing zone re-centered); SCALE SLOTS grow 4→**16** (A–D primary row + bank rows E–J / K–P of six each, mirror-centered). Plans amended to match: `harmonic_layer.md` D1/GATE-A.1 slot count 16 (capture cost noted: ~16.5 KB/snapshot, ~1.05 MB across 64 — small next to the reel; text export skips empty slots; `scale_slot` destination rounds 0–15), `seq_scale_sidecar.md` slots section + symmetry note. Twin 216×216 beds, mirrored rows, and the mirror-line commit column unchanged. | SLB |
| 2026-07-06 | 80 | **Harmonic/notation arc PLANNED — two plans authored, GATE A cleared at ALL recommendations** (owner: "create the execution plans with all recommendations. first, make the layout symmetrical, ensure things align well with other items on the interface"). Design lineage: owner's "scale setting/modulation of scale + novel mini-notation UI" think (scale = polygon on a pitch-class ring, Coltrane's tone circle as input; rhythm = the same geometry on the cycle ring). (1) `Plans/harmonic_layer.md` — engine: 4 scale slots per destination with slot-0 back-compat, `scale_root` ±24 quantized + `scale_rotate` as modulation/matrix destinations (`pattern pitch_scale_slot` = chord progressions on the existing cycle clock), morph scale-blend rule = stochastic source-pick, `scope_tap scale`/`pattern` on the existing outlets, schema v5. (2) `Plans/seq_scale_sidecar.md` — surface: generated third canvas `pd/ligase_seq.pd` with TONE CIRCLE (ring-order lenses CHRO/5THS/W-T; Messiaen polygon presets via AXIS; AXIS→SLOTS = Giant Steps generator), TIME CIRCLE twin (Euclid K/N/ROT + amber playhead), SLOTS/COMMIT column (cold edit + APPLY, truth-in-labeling SENDS readouts), PATTERN GRID 8×16 (pin-at-VALUE; XPNDR addressing). Symmetry directive: twin 216×216 beds = the joystick/scope module, mirrored control rows at identical y, commit column on the mirror line — mockup `docs/ui/ligase_seq_panel.svg` committed (generator `gen_seq_mockup.py` is a plan-stage artifact, retired at build Step 1 when the layout folds into panel_layout.py). Build order: harmonic layer first (sidecar depends on its Steps 1-2). | SLB |
| 2026-07-06 | 79 | **Housekeeping true-up** (owner: "update RESUME.md, overwrite previous contents. ensure QUEUE is up to date"). RESUME.md rewritten from scratch to the Seq-79 state (panel-as-instrument + bundle + scope taps + deferred license; conventions gain the generated-artifacts rule, the class-construction trap, and the `lgR_<id>` scripting harness). QUEUE §2 ON-DECK prototyping-arc block trued up to actual statuses (panel steps 1-5 DONE / scope taps DONE / VST at GATE A with license deferred) — it still described all three at GATE A — and §0 gains the 2026-07 feature-arc paragraph (it ended at the June bug arc). No plan-status or decision changes; §4a already current. | SLB |
| 2026-07-06 | 78 | **`.plugdata` bundle SHIPPED** (`Plans/pd_panel_prototype.md` Step 5 → DONE; only Step 6 owner hands-on remains). `make bundle` → `docs/ui/emit_bundle.py` → `dist/ligase.plugdata`: panel + expander patches + `pd/README.md` + `ligase.conf` + the compiled external(s) present (darwin rides along when built on the Mac), under ONE `ligase/` package dir with a `meta.json` whose keys match plugdata's PatchInfo reader (`FolderName: ligase` pins the install path — otherwise the loader hashes title/author/version into a slug). Format verified against the ACTUAL loader source (PluginEditor.cpp `installPackage()`: .plugdata = ZIP; each top-level dir moved into `Patches/`; `__MACOSX` skipped; quarantine warned). GATES: deterministic build (rebuild sha256-identical: efc9ee32…), structural verify mode (`--verify`: single top-level dir, meta keys, patch byte-identity vs `pd/`, external present), and an install SIMULATION — unzip + load the panel from the package dir: zero errors, external + `ligase.conf` both load from the package ("Loaded max_grains = 200 from ligase.conf" — the reason the conf is bundled). `dist/` gitignored (owner builds locally; a downloaded bundle would hit Gatekeeper unsigned-binary quarantine — noted in README). | SLB |
| 2026-07-06 | 77 | **VST GATE A.2 (license) DEFERRED by owner** ("I can set the license later, no one is using it but us right now"). GPL-2-only stands for now; personal-use builds (plugdata standalone, the .plugdata bundle of our own code, a private compiled-in plugin build) are unaffected — the GPL-2/GPLv3 incompatibility only bites at *distribution* of a combined plugdata/VST3 build, so the decision is re-raised at that point, not before. `Plans/vst_plugin.md` header + §4a row updated. | SLB |
| 2026-07-06 | 76 | **Pd panel prototype steps 1-4 BUILT + headless-verified** (`Plans/pd_panel_prototype.md`; Step 5 `.plugdata` bundle + Step 6 owner hands-on remain). `gen_panel.py` refactored into **`panel_layout.py`** — the surface AS DATA (113 control records: id → `lgR_<id>` receive symbol, svg args, binding, ENGINE-unit lo/hi/default per the update_inlets validation windows, init-send flag; MATRIX_SRCS/DSTS from the engine's mod_source/dest vocabulary; SHAPE_MEANINGS per-family RATE/A-D table; XPNDR 8×8 PAGE×PARAM field map; DIST_PRESETS bundles) — + `emit_svg.py` + `emit_pd.py` (gen_panel.py = thin wrapper). Emitted **`pd/ligase_panel.pd`** (3328 objects / 3080+ connections: 22 CV knobs → pack/line~ → signal inlets under the headless-0 contract; generated 16×22 tgl matrix with DEPTH-at-pin ± polarity; SOURCE SHAPE FAMILY×INST routing layer; joystick → IN 23/24; splice select; openpanel/savepanel reel I/O; 10 Hz poll) + **`pd/ligase_xpndr.pd`** (423 objects; speaks only the lg_engine/lg_state9 buses) + `pd/README.md` (scripting/MIDI hook; honest seams: `splice_finish_nav` is the 0/1 flag not a jump — B-item candidate; MASTER knob deliberately unwired; GAUS/EXP silkscreen-forward). GATES MEASURED: SVG **byte-identical** through the new pipeline; both patches load headless zero-error; knob→snapbuf readback (grainsize 0.777 / speed 2.5); matrix pin round-trips matrix_dump (lorenz1→speed depth −2; unpin = disabled slot); joystick → MORPHXY 0.9 0.8; XPNDR canvas full round-trip incl. band source/instance codes; per-family SOURCE SHAPE sends land (lorenz sigma, noise_freq, sphere kick, dist preset bundle, recinput arm). Owner panel geometry landed with it: **SCOPE display = exact 216×216 twin of the joystick pad, top-aligned** (TAP/VIEW/OUT badge moved to a right column) and **PRESETS grow to 4×8 = 32 slots** (silkscreen 1-32 → engine snapshot slots 0-31; verified snap25+STORE → "snapshot 24 captured", snap32+RECALL addresses slot 31). Regression exact (RMS 0.372309 / buffer 0.330109). | SLB |
| 2026-07-06 | 75 | **Pd panel prototype GATE A cleared at the recommendations — build started.** plugdata dialect, two canvases (main + XPNDR subcanvas), fully generated (emit_pd), DEPTH-at-pin matrix policy, headless-0 all-inlets-driven, 10 Hz poll, snapshots-as-presets. Layout truth = the current gen_panel.py (single chassis + SCOPE card + integrated expander), not the plan's older prose. | SLB |
| 2026-07-06 | 74 | **Scope taps IMPLEMENTED + headless-verified** (`Plans/scope_taps.md` -> DONE). scope_x~/scope_y~ signal outlets 10/11 (dsp_add 28->30, all four perform returns bumped -- the morph-CV crash class explicitly re-checked with a dsp 0/1 restart run; early-outs zero-fill). `scope_tap <11 families>` (default lorenz 1): generators S/H per tick through the SAME readout paths as the matrix (shaping params visible; rand tap reads the LCG without advancing the shared seed); chaos/physics = x/z bipolar. The GRAIN CONSTELLATION ships: one active grain per sample, X = splice position, Y = env x amp via the render's envelope table; idle beam parks at exact (0,0); grainsum = tanh(sum/4) silhouette. MEASURED: attractor both-lobes (X sd 0.407/0.387 across two independent captures), sine tap sd = 1/sqrt2 on a perfect 2 Hz ramp (sd 1/sqrt3), sphere_spin 5 -> 0.80 Hz quadrature orbit (|corr| 0.996 at quarter-period lag), constellation X spans the configured grainstart range with a U-shaped cosine-envelope Y histogram, grainsum 0.235->0.554 for maxgrains 2->8. Monitor-not-state (never captured/morphed). Regression exact baseline; no CPU delta; warning-free. Verification footnote: `play` (bare) STOPS -- drive tests with `play 1` (the flat first capture doubled as the parked-idle-beam negative control). Remaining: owner feel test with [oscilloscope~] + the Pd-panel wiring (Plans/pd_panel_prototype.md). | SLB |
| 2026-07-05 | 73 | **Scope-taps GATE A cleared at the recommendations — implementation started.** Signal outlets scope_x~/scope_y~ (10/11 appended), scope_tap tap table (chaos/physics x/z XY via existing per-axis readouts; waveforms readout+sweep), grain constellation (default) + grainsum, monitor-not-state, default tap lorenz 1. | SLB |
| 2026-07-05 | 72 | **Scope-taps plan authored + panel SCOPE card drawn** (owner: route modulation signals via a switch near SOURCE SHAPE to an oscilloscope display + a new grain-state output signal). Grounded: the per-axis readouts ALREADY EXIST (lorenz_get_normalized(state, axis), sphere/nbody mode readouts + the spatial vec3 helpers) — the 'most visually interesting output per type' is a tap TABLE, not new math; outlets today = 9, so scope_x~/scope_y~ append as signal outlets 10/11 (dsp_add 28->30 + four perform-return bumps — the documented morph-CV crash trap flagged in the plan). New representation: the GRAIN CONSTELLATION — round-robin one-active-grain-per-sample scan, X = position-in-splice (±1), Y = env×amp — the granular engine drawn live on an XY scope (position scatter = organize/grainstart, height = envelope, density = voices/IOT; empty pool parks at center); plus a grainsum silhouette tap. Panel: SCOPE card replaces the messages card (which returns to the manual) directly under SOURCE SHAPE — 202×148 phosphor bed with a GENUINELY INTEGRATED Lorenz x/z trace (σ10 ρ28 β8/3 in the generator), TAP FOLW/GRAIN + VIEW XY/SWP switches, OUT 10/11 badges; SOURCE SHAPE legend notes FAMILY×INST feeds the scope. Scope routing = monitor, not voice state [R]. Plan at GATE A. | SLB |
| 2026-07-05 | 71 | **Prototyping/UI/VST arc QUEUED — two plans authored at GATE A** (owner: 'begin QUEUEing prototyping and UI and vst execution plans'). (1) `Plans/pd_panel_prototype.md` — the panel as a working patch via layout single-sourcing: gen_panel.py -> panel_layout.py + emit_svg + emit_pd (SVG = silkscreen, .pd = instrument, provably the same surface); knobs -> line~-smoothed inlets under the headless-0 hardware contract; generated pin matrix with DEPTH-at-pin-time; XPNDR canvas as the snapbuf API's acceptance test; owner hands-on gate last. (2) `Plans/vst_plugin.md` — staged: v1 = plugdata fork with ligase COMPILED IN (web-verified: no runtime external loading in plugdata-plugin/Camomile; documented CMake+Setup.cpp mechanism; pinned-tag vendoring; automated-test-procedure-inside-plugdata as the engine-identity gate; B7 visibility/kiss_fft static-link hazard flagged), panel = GUI, ~16 host params; v2 = native JUCE engine extraction deferred to its own plan (engine facade over the already-modular core; class_addmethod table = the parameter API; schema-v4 text = preset format). LICENSE finding: LICENSE is plain GPL-2, no or-later grant in file or headers — incompatible with GPLv3 plugdata/VST3-SDK for DISTRIBUTION; owner relicense decision at the VST plan's GATE A.2. No code yet. | SLB |
| 2026-07-05 | 70 | **Source shapes IMPLEMENTED + headless-verified** (commit fe90c79; `Plans/source_shapes.md` -> DONE). Every generator's shape is now settable: waveform_phase/square_pw/saw_skew (readout-side — the shared per-instance phase advance untouched, sine/saw/square n stay phase-locked; defaults proven bit-identical via HEAD-vs-new md5-identical WAVs), lorenz_sigma/rho/beta (rho = chaos knob: tail variance collapses ~3800x at 15 vs 28; rho-60 soak finite, 0 NaN), sphere_spin (energy-neutral velocity curl, |v| conserved to 1e-6; pan_mode 2 + spin sweeps the stereo field periodically) + sphere_kick_rand (event). Capture schema v4: 28 scalars via the walker (since=4), text 3->4 with v1-v3 compat, binary 2->3, MORPH_SCALAR_COUNT 64->96 with version-gated exclude-index REMAP on import (old exports select the same fields — explicitly tested); sources group covers all; expander addresses the names for free. Panel legend drops the * marks. | SLB |
| 2026-07-05 | 69 | **SOURCE SHAPE panel cluster designed + plan authored** (owner: expose everything about the shape of the modulation itself). Grounded inventory: nbody orbit + sphere damping/elasticity/kick existed (v3 state); waveform phase/PW/skew, lorenz sigma/rho/beta setters, and sphere spin did NOT exist in the engine. Panel (e6599cf): the Plaits/Synthi multi-engine idiom — FAMILY (9-pos incl. FOLW) x INST cursor -> RATE + A-D assignable knobs with a printed per-family legend matrix + KICK!/RESET/MODE; replaces the MOD SOURCES strip; engine-gap knobs marked * pending build. `Plans/source_shapes.md` authored with GATE A taken at [R]: readout-side waveform shaping, raw clamped lorenz setters (no macro), spin = velocity curl. | SLB |
| 2026-07-05 | 68 | **Expander v1.1 audition/compare SHIPPED** (owner: 'open a PR for the audition/compare and then merge'). The GATE A.1/A.3 deferred pair, implemented as the explicit opt-in on the landed v1 machinery: `snapbuf_audition 1` captures the live voice to a revert slot (modulation-transparent) then applies the buffer through the masked-restore path; `snapbuf_audition 0` restores the pre-audition voice EXACTLY; `snapbuf_compare` = A/B toggle over the same latch; `snapbuf_apply` mid-audition COMMITS (revert discarded, subsequent release = no-op); buffer edits made while auditioning stay cold; excluded fields stay live in both directions. Verified headless via from_live+get probes: audition-on reads the buffer's 2000, release reads the pre-audition 900 exactly, commit reads 1234 with release a no-op. Regression exact baseline; warning-free. Panel: XPNDR COMMIT strip -> COMMIT/AUDITION (STORE/ASSIGN/AUDITION/A-B, hold = momentary). Manual audition paragraph + quick-list. Branch then PR'd -> main and merged per owner. | SLB |
| 2026-07-05 | 67 | **Snapshot expander v1 IMPLEMENTED + headless-verified** (commit 2644a57; `Plans/snapshot_expander.md` -> DONE). One shared `morph_fields[]` walker (150 entries, `since`-versioned, _Static_assert-bound) now drives export/import AND the new snapbuf get/set/dump — no schema duplication. Edit buffer: `snapbuf_load/_from_live/_set/_get/_dump/_store/_apply/_clear`; message-thread only, never read in perform; apply = the only realtime touchpoint (morph_restore path); STORE keeps surface placement (placed slot reshapes a 50/50 blend 0.6->0.3 next block; unplaced changes nothing). Schema v3: the sources (noise_freq_1..4, env_follow_ms, sphere/nbody physics + mode discretes) join capture/restore/export; text 2->3 with v1/v2 compat (newer-`since` fields keep current values); binary version 1->2 with explicit refusal; `morph_exclude sources` = global weather. Deliberate alignment: snapshot_recall now honors the selection tree via the shared morph_mask_excluded() (blend/recall/apply = one ownership boundary; no-op when nothing excluded). ACs: cold-edit BYTE-IDENTITY (25 edits + 149-line dump during recording, WAV md5 unchanged), apply==recall, v3 round-trips diff-empty, dump->replay reconstructs in a fresh instance, changed noise_freq_2 travels with the voice; tests/morph suite clean; regression exact baseline; warning-free. Remaining: owner workflow feel (AC6) + expander v1.1 (audition/compare pair). | SLB |
| 2026-07-05 | 66 | **Expander GATE A fully cleared — implementation started.** Owner approved decisions 1-6 at their [R] recommendations (cold-only v1, audition+compare deferred as the v1.1 pair; scale lists message-edited; per-subfield get/set + import-grammar lines; STORE keeps surface placement; one buffer) on top of the Seq-65 decision 7 (schema v3). Build in flight: Step 1 walker unification + generator params into morph_snapshot_t/capture/restore (text schema 2->3, v2 import compat, `sources` selection-tree group), Step 2 edit buffer + whole-buffer verbs, Step 3 snapbuf_set/get/dump, Step 4 docs. | SLB |
| 2026-07-05 | 65 | **Expander GATE A.7 OWNER-APPROVED: generator params join snapshots (export schema v3).** Owner: it makes the most sense — 'params are weather control'; each voice carries its own weather. Plan Step 1 updated to carry the v3 scope inside the walker unification (noise_freq_1..4 / env_follow_ms / sphere + nbody physics into morph_snapshot_t + capture/restore + walker; v2 import compat = missing generator fields keep current values; sources group registered in the morph selection tree so `morph_exclude sources` restores global weather). Decisions 1-6 remain open at [R]. | SLB |
| 2026-07-05 | 64 | **MOD SOURCES exposed on the panel + expander GATE-A grows the capture question.** Owner spotted the modulation sources' own params were nowhere: `noise_freq_1..4` (per-instance rate scales — grounded finding: generator phase advances by `IOT x scale` (grain.c), so sources breathe with grain density, and instance n drives SIN/SAW/SQR/PERL n together), `env_follow_ms`, and the sphere/nbody physics params are all message-only (no inlets -> fell outside the original panel brief) AND absent from `morph_snapshot_t` (verified zero generator fields -> the expander had nothing stored to show; a snapshot's motion CHARACTER is global weather, not voice state). Panel: new MOD SOURCES strip (RATE 1-4 / FOLLOW / sphere DAMP+ELAST / nbody G+DAMP, with the iot-proportional + global-rate legend) in the right column between the matrix and morph. Plan: `Plans/snapshot_expander.md` GATE A gains decision 7 — capture generator params in snapshots (export schema v3, folded into Step 1's walker unification) [R] YES, with the global-weather behavior recoverable via `morph_exclude`; source-rates-as-matrix-destinations noted as future matrix-domain work. | SLB |
| 2026-07-05 | 63 | **Snapshot-expander plan authored + XPNDR sidecar panel drawn.** `Plans/snapshot_expander.md` (GATE A): an EDIT BUFFER 'sidecar module' — one extra `morph_snapshot_t` never read by `ligase_perform`; `snapbuf_load/_from_live/_set/_get/_dump/_store/_apply/_clear` over a field walker UNIFIED with the morph text-export schema (Step 1 = factor the export/import per-field table into one shared iterator — prevents schema drift); apply = `morph_restore` path (recall-equivalent precedence, selection tree honored); STORE to a placed slot deliberately reshapes the morph field next block. GATE-A: cold-only v1 [R] (audition/compare = v1.1 pair), scale lists message-edited [R], per-subfield get/set [R], one buffer [R]. Depends on v1.5 capture transparency (from_live grabs bases, not wobble). Panel: `docs/ui/ligase_synthi_panel.svg` gains the XPNDR sidecar case (SNAP LED + DATA/LOAD/FROM-LIVE, PAGE×PARAM 8×8 address selectors, VALUE LED readout, reusable band-edit cluster MIN/MAX/SLEW+SOURCE/INST+ENABLED/INVERT, STORE/ASSIGN cold-edit commit) AND the Presto-Patch matrix grew the six per-grain columns (22×16 shown, dashed v1.5 divider) now that the engine accepts them. | SLB |
| 2026-07-05 | 62 | **Modulation matrix v1.5 IMPLEMENTED + headless-verified** (commit 30235fb) — per-grain destination tier + capture transparency, per the `docs/modulation_layers.md` contract (R1-R5) authored this session from the owner's snapshots×metasurface×matrix interaction question. Six per-grain dests (speed/grainsize/grain_start/amplitude/pan/pitch_fine, alias grainstart): sums cached once per block, applied FUNCTIONALLY in `scheduler_trigger_grain` after the range sample / before the in-place clamps, bitmask-guarded, NEVER writing the shared fields (inlets keep ownership; capture clean by construction). Speed composes with the pitch override (midi 67 holds inc 1.4983; +sine1 depth 0.2 -> 1.4983..1.6983 exact); pitch_fine 0.5 -> inc 1.0293 = 2^(0.5/12). Capture transparency: morph_capture reads mod_base when a connection drives a captured scalar — scanrate wobbling at -0.176 live snapshots as exactly its base 1.0; grounded finding: only scanrate was matrix-dirty in v1 (organize/sos/iot capture message-stored bases; env_skew's live field isn't a captured scalar). Regression byte-exact (RMS 0.372309 / buffer 0.330109 / test_delay clean); clamps saturate without NaN; warning-free build. `docs/modulation_layers.md` -> SHIPPED (as-built). | SLB |
| 2026-07-05 | 61 | **Five remaining direction plans IMPLEMENTED + headless-verified** (owner: "complete all execution plans on the queue, follow automated test procedure"). Each built on its plan's recommended [R] GATE-A options, one commit per feature, and gated on the AUTOMATED_TEST_PROCEDURE (exact-baseline every time: RMS 0.372309, buffer L/R 0.330109) + test_delay.pd + warning-free `make clean && make`. (1) **Polyphony** 7238cb9 — scheduler voice pool (MAX_VOICES=8, steal-oldest, budget=global-cap+documented), `poly <0|1>` + `chord ...` + vel-0 note-off via channel-aware `midi`; per-voice transposition threaded through `scheduler_trigger_grain(...,voice_note,voice_active)`; 3-note chord seats increments 1.000/1.260/1.498; smear stays mono. (2) **Spatial** 2b8df2c — pan_mode 2 + `spatial <sphere|nbody> [inst] [body]`; per-grain frozen 3D snapshot -> front-biased azimuth -> constant-power gains precomputed at trigger (2 mul + 2 add/sample); `spatial_width` 0->L==R exactly (diff-RMS 0.000000), 1->real spread tracking the orbit; pan_mode 1 md5-identical to the pre-change binary; new `*_get_normalized_vec3` pair. (3) **Modulation matrix** bc45503 — MOD_MATRIX_MAX=32 flat conns, 44 sources (gens x4 + pattern0-7 + env_l/r/mono peak follower, `env_follow_ms`) -> 20 per-block dests incl. modout1-4 (compound-gate rewrite, matrix-only emits 0.5+sum); bipolar depth in dest units; apply-site clamps via bounds table; `mod_track_base()` beyond plan (pins base for self-read dests so matrix-only drive doesn't integrate); exact-math verified (cutoff 5735.31 noise / 1800 silence). (4) **Pattern events** fdc13b1 — `pattern event|trigger grain/splice/retrig/gate/bang`, kind tag in a parallel per-slot array written before the step_count publish barrier; free-scan `pattern_alloc_event_slot` (param allocator NULL-derefs — never called with NULL); Euclid `1(3,8)` Bjorklund -> canonical x..x..x. (re-confirmed independently), `v*N(k,n)` rejected; `rev` parse-time. (5) **Resonator bank** a737b30 — new `src/grain_smear_bank.{c,h}` (rep (a) + Shape A: whole grain_smear voices verbatim, mix=1 pure-wet, 1/N pre-scale, chunked scratch), `smear_mode <0|1>` + `smear_bank_mix/feedback/resonance/stages`, tuning = smear_pitch_scale degrees via the P1 note->Hz per block; A-major -> tuned peaks 28-36 dB over off-tune controls (Goertzel, independently re-measured); mode-0 md5-identical to pre-change binary; bounded at fb 0.95/res 0.99 (0 non-finite). Remaining on all five: owner ear-tests (chord balance/maxgrains, orbit feel, matrix musicality, burst character, bank timbre = GATE A.7 v1-vs-KS input). pd 0.54.1 + sox installed in this env for the test procedure. | SLB |
| 2026-06-25 | 60 | **Morph / Metasurface COMPLETED — v1.1 kernels/cursor + selection tree + persistence** (built out the rest after the v1 core at Seq 59; commits 1243aee..5261504). PERSISTENCE (3 routes): morph_save/morph_load (binary .morph = whole morph_state_t + magic/version/size header), morph_export/morph_import (human-readable .txt, LOGICAL-field schema so it survives struct-layout changes across builds; version 2), morph_state (re-sendable message dump of the layout to outlet 9). SELECTION TREE: morph_include/morph_exclude gate what the blend APPLIES (capture stays full) — by param (amplitude/pan/moog_*/smear_*/gdelay*), by group (pitch/smear_pitch/fx), or by raw included[] index; the excluded-fields get the current live value patched in so morph_restore is a no-op for them. Persisted across ALL THREE routes (binary struct, text 'exclude <idx>' line, morph_state emits morph_include all + morph_exclude <idx>). KERNELS: natural-neighbour now ships (morph_interp 1) as a MESH-FREE sampled Sibson approximation (steal-area on a 48x48 grid) — verified measurably more LOCAL than IDW (587 vs 566 Hz at an asymmetric 3-snapshot interior point). CV CURSOR: morph_cursor 1 hands the cursor to two new signal inlets (Pd index 22/23 — appended so existing inlet numbers are stable; dsp_add 26->28, the 4 perform returns bumped w+27->w+29, a missed return was a real DSP-start crash, caught+fixed). Verified headless tests/morph/MC1-10 + MS/MT/EX (capture/blend/route/persist/exclude/CV-sweep 440->880/NN-vs-IDW) + a HAND-AUTHORED 'imagined' surface that imports correctly (gen_imagined.py). warning-free; test_delay.pd clean throughout. Remaining (minor): FX scalar bases for distortion-enhancement/stut/bencina (their bands already morph). | SLB |
| 2026-06-24 | 59 | **Morph / Metasurface v1 IMPLEMENTED + headless-verified** (`Plans/morph_metasurface.md` -> v1 COMPLETE; first direction plan built). New module `src/morph.{c,h}` (types + IDW kernel + curve helpers; pure, x-independent) + the x-coupled capture/apply/handlers in ligase~.c. Gates: B (skeleton), 2a (45 modulation-band capture/recall via one fixed-order pointer table), 2b-1 (21 scalar bases + 30 discretes + 2 scale lists; enums by value to avoid int* aliasing), 2b-2 (11 playable-FX shadow mirrors moog/smear/gdelay -> GATE C complete), D (morph_apply_at IDW blend: continuous weighted-sum + discrete argmax + exact-hit verbatim, applied via morph_restore; morph_point/morph/morph_x/_y/morph_interp/morph_power), E (morph_route/_run/_stop/_pause + per-block stepper in ligase_perform before update_inlets), docs (manual MORPH/METASURFACE section + quick-list). GATE-A taken = the [R] recs (IDW v1 behind a kernel-agnostic API; float cursor now; argmax discretes; shadow FX; stepper before update_inlets; full capture; caps 64). Verified (tests/morph/MC1-5): band restore, scalar+discrete restore (smear 880), blend (morph 0.5 -> semitone 6=622.25Hz + amp band 0.4/0.9), route sweep (440->880 in 303 steps). 6 commits 4799efc..de6d4bb; warning-free; test_delay.pd clean. Deferred v1.1 (documented): natural-neighbour kernel, per-field include/exclude, CV signal-inlet cursor (GATE A.4), FX bases for dist-enhancement/stut/bencina. | SLB |
| 2026-06-24 | 58 | **Six forward-direction execution plans authored** (owner-requested exploration of where the synth could go). Via two research+design+adversarial-verify workflows (incl. web research on Ross Bencina's NIME-2005 Metasurface paper — confirmed Natural-Neighbour/Sibson interpolation, and that AudioMulch's routes are bare X/Y automation so per-leg rate/curve is ligase's value-add). Plans, at GATE A, priority order: (1) Morph/Metasurface (Plans/morph_metasurface.md — snapshot interp + coordinate cursor + route waypoints; v1 IDW kernel behind a kernel-agnostic API, NN deferred); (2) Polyphony (Plans/polyphony.md — chordal granular, voice pool + N-transposition over the ONE shared playhead, Reading A; B=parallel instances; playhead untouched); (3) Spatial granulation (Plans/spatial_granulation.md — nbody/sphere 3D trajectories -> per-grain placement; v1 stereo/binaural, multichannel a separate v2 class-construction change); (4) Modulation matrix (Plans/modulation_matrix.md — N->M connections+depth, summed, + envelope-follower input source; onset/pitch v2); (5) Event-firing patterns (Plans/pattern_events.md — step fires grain-burst/splice/retrig/gate/bang + Euclid (k,n) + rev); (6) Resonator bank (Plans/resonator_bank.md — distinct mode, N tuned resonators reusing the smear allpass excited by the granular bus; KS weighed as v2). Verifiers caught real subtleties (morph stepper-vs-update_inlets CV precedence; matrix modout compound-gate; spatial multichannel=class-construction; poly grain-pool budget soft-cap-4-vs-pool_size; resonator CPU). Placed §2 ON DECK + §4a. No code yet — each carries a substantial GATE A for owner sign-off. | SLB |
| 2026-06-24 | 57 | **param_lock value-report wired for all modulatable params** (owner audit). Confirmed param_lock STOPS modulation for every new param (smear_*/stut_reps/bencina_*/pitch_fine/smear_pitch_fine/modout*) — automatic via get_param_range_by_name -> enabled=0 (empirically 11/11 stopped, 0 unknown). Gap found + fixed in the SECOND half (the outlet-9 current-value report): get_current_value had per-param *_current cases that missed the newer params, so it reported 0.000. Added a universal fallback — for any param with a range, return param_range.smoothed_value (the exact value sample_param_range last returned). One line covers smear_frequency/_resonance/_stages/_feedback, bencina_pan, pitch_fine, smear_pitch_fine, dist_*, modout1-4. Verified: param_lock smear_frequency -> 1602.197 (in [200,2000]); smear_pitch_fine -> -0.033 (in [-0.5,0.5]) (were 0.000). warning-free; test_delay.pd clean. Tests in tests/param_lock/. | SLB |
| 2026-06-24 | 56 | **Manual documents the 4 new features** (`docs/ligase_manual.md`, `.md` only — PDF held). SMEAR > Smear Pitch (resonator note->Hz: smear_pitch_source/semitones/smear_note/scale/rand_type/pattern smear_pitch, override precedence, [20,0.45sr] clamp); PITCH & SPEED > Fine Tune (pitch_fine cents, modulatable) + two-destination model + Channel-Aware MIDI Routing (midi/midi_channel/pitch_channel/smear_pitch_channel, unison vs separate, inlet coexistence); PLAYBACK CONTROL > One-Shot Playback (loop/trigger, grains-uncut, nav-wins); MESSAGES quick-list groups + pitch_fine/smear_pitch_fine in the MODULATION targets list (+124 lines). | SLB |
| 2026-06-24 | 55 | **One-shot playback (stop-at-splice-end) IMPLEMENTED + headless-verified** (`Plans/oneshot_playback.md` -> DONE). `loop_mode` on splice_behavior_t (default 1=loop). SCANNING + CLOCK_ADVANCE wrap branches stop (is_triggering=0/is_playing=0; SCANNING breaks the per-sample loop) when loop_mode==0 and no nav queued (NAV WINS via a had_pending_nav flag captured before pending_splice is consumed). ligase_bang gates clock_bang_received on is_triggering (clock coherence). New selectors loop <0|1>, trigger (no-arg re-arm; bang is the BPM clock). Grains uncut by construction (render block untouched). Verified (tests/oneshot/OS*.pd): loop 1 -> 29 bangs (loops), loop 0 -> 1 bang (stops), loop 0 + trigger -> 2 bangs (re-arm). warning-free; test_delay.pd clean. | SLB |
| 2026-06-24 | 54 | **Channel-aware MIDI routing (P2) IMPLEMENTED + headless-verified** (`Plans/midi_channel_routing.md` -> DONE) — **pitch-destination arc now COMPLETE (P1+P2+P3).** `grain_midi_channel`/`smear_midi_channel` on scheduler_t (default 1/2); `midi_msg_active` latch on ligase_t. New `midi <note> [vel] [channel]` (A_GIMME, fed from [notein]) routes by channel to the two destinations via two independent if-tests (same channel=unison, different=separate); `midi_channel`/`pitch_channel`/`smear_pitch_channel` setters; the inlet-19 signal-MIDI read gated on !midi_msg_active (the message owns grain once used; inlet byte-identical otherwise). Verified (tests/pattern/P2_midi.pd): separation (ch1->grain bang, ch2->smear 880Hz), drop (ch3), unison (midi_channel 1 1 -> one note fires both grain bang + smear 196Hz), note/channel range rejection. warning-free; test_delay.pd clean. | SLB |
| 2026-06-24 | 53 | **Pitch fine-tune (P3) IMPLEMENTED + headless-verified** (`Plans/pitch_fine_tune.md` -> DONE) — Moog-style +/-50-cent (+/-0.5-semitone) modulatable fine offset on BOTH pitch destinations (P1 landed, so both halves shipped). pitch_fine + pitch_fine_range on pitch_control_t (per-grain apply in scheduler_trigger_grain, recompute final_speed once so it reaches all modes incl OFF, base=pitch_fine not 0); semitone_fine on smear_pitch_control_t + smear_pitch_fine_range on scheduler_t (per-block apply in P1's smear stanza before the powf). Both registered as param_range targets (get_param_range_by_name + rand_type all-list); pitch_fine/smear_pitch_fine <cents> setters (cents/100 -> semitones). Verified (tests/pattern/SM4-6): smear +50/-50/+25c -> 452.89/427.47/446.40 Hz, grain +50c on degree 0 -> semitone 0.50, modulatable (param_range -> Hz wobbles in [427.5,452.9]), units correct (50c=0.5 semitone not 50), fine 0 = backward compat. warning-free; test_delay.pd clean. Next: P2 (channel MIDI). | SLB |
| 2026-06-24 | 52 | **Smear pitch destination (P1) IMPLEMENTED + headless-verified** (`Plans/smear_pitch.md` → DONE) — the resonator can now be PLAYED. New `smear_pitch_control_t` (dedicated, not `pitch_control_t`) + `smear_pitch_source_t` in types.h; A440 defaults in `scheduler_create`. Per-block source→semitone→Hz (`ref·2^(semitone/12)`) in the smear stanza with **override** (the `smear_frequency_range` branch gated on `!smear_pitch_control.enabled` — true bypass, consistent with grain's `speed_range` bypass; clamp stays in `smear_update_coeffs`). De-static'd `sample_scale_semitones` for the SCALE source. Messages: `smear_pitch_source`/`smear_pitch_semitones`(auto-SEMITONE)/`smear_note <note> [ref_note] [ref_hz]`/`smear_pitch_scale`/`smear_pitch_rand_type`/`smear_pitch_debug`. `pattern smear_pitch` on reserved slot 6 (param auto-allocator shrunk to 0..PATTERN_SLOTS-3) + clear. **Verified** (`tests/pattern/SM*.pd`): semitone 0/12/-12→440/880/220, `smear_note 60`→261.63, pattern [0 4 7]→440/659.26/880 (wrap+octave, tempo-locked), SCALE→random scale notes, override pins to the note (range bypassed), source 0→backward compat. warning-free; `test_delay.pd` clean. Dropped `smear_pitch_range` (unused). Next: P2 (channel MIDI) / P3 grain-fine. | SLB |
| 2026-06-24 | 51 | **Smear pitch + dual-destination MIDI scoped** (owner request: MIDI/modulation pitch for the smear resonator; channel-routed separate pitch for grain vs smear; pattern-on-one/MIDI-on-other). Two plans authored via a research+design+adversarial-verify workflow (3 grounding agents over smear / pitch+MIDI / pattern-pitch surfaces; design; per-plan verify — which confirmed all file:line refs real, caught that `sample_scale_semitones` is `static`, corrected a scheduler member-placement claim, and cross-checked the smear pattern-slot reservation). Architecture: two independent pitch DESTINATIONS (grain→speed exists; smear→`smear_frequency` Hz new), each with a selectable source. **P1** `Plans/smear_pitch.md` — dedicated `smear_pitch_control_t` (deliberately not `pitch_control_t`), note→Hz `ref·2^((note−ref)/12)` applied once-per-block in the `:908-925` smear stanza with OVERRIDE precedence, `pattern smear_pitch` on reserved slot 6 (auto-allocator shrunk to 0..5), the clamp stays solely in `smear_update_coeffs`. **P2** `Plans/midi_channel_routing.md` — channel-carrying `midi <note> [vel] [channel]` message (signal inlet 19 carries no channel) + channel→destination map (same=unison, different=separate) + inlet coexistence via a `midi_msg_active` latch; depends on P1. Backward compat load-bearing. Plus **P3** `Plans/pitch_fine_tune.md` (added same session per owner): a modulatable **±50-cent (±0.5-semitone) Moog-style fine tune** per destination — `pitch_fine`/`smear_pitch_fine` as `param_range` targets (B19 five-touchpoint recipe), grain fine sampled per-grain in `scheduler_trigger_grain` (after the pitch switch, before `last_semitone`), smear fine per-block riding P1's note→Hz semitone; grain half independent, smear half forward-deps P1; verify caught a ±25-vs-±50-cent AC units slip (→452.9/427.5 Hz). **Owner GATE-A decisions folded:** A440 ref; P1 full menu incl. SCALE (de-static `sample_scale_semitones`); channels independent (grain 1/smear 2); smear precedence = override (consistent with grain, no "combine" mode — that was an invented option, dropped as redundant with the fine-tune); fine = cents at the message / semitones in the range. Placed §2 ON DECK + §4a. No code yet. | SLB |
| 2026-06-24 | 50 | **One-shot playback scoped** (owner request) — plan `Plans/oneshot_playback.md` authored via a research+design workflow (2 grounding agents over the playback/playhead/grain-lifecycle + play/splice message surfaces, design, adversarial verify — which confirmed every file:line ref real and the grain-decoupling claim sound). Architecture: a composable `loop <0|1>` flag (default 1=loop; 0=oneshot stop-at-end) on `splice_behavior_t`, NOT a 4th playhead mode — oneshot is orthogonal to how the head moves, per the owner's "combination of modes". The two SCANNING/CLOCK_ADVANCE `wrapped` branches become `is_triggering=0` stops when `loop_mode==0 && pending_splice<0`; a `trigger` no-arg selector re-arms from `splice_start` (`bang` is the BPM clock, unusable). **Crucial finding: "grains finish on their own" is already implemented** — the render block (`scheduler_process`) runs unconditionally, never gated on `is_triggering`, so stopping the transport never cuts active grains; `play 1` already starts at `splice_start`. New surface is tiny: 1 field + init, 2 perform one-liners, a `&& is_triggering` clock-gate in `ligase_bang`, 2 selectors. Placed §2 ON DECK + §4a. **GATE A owner-approved** (all recommended: field on `splice_behavior_t`, `pending_splice` nav-wins, STATIC no-op, `loop`+`trigger` selectors, clear both `is_triggering`+`is_playing` on stop) — cleared to Step 1. No code yet. | SLB |
| 2026-06-24 | 49 | **Pattern P3 IMPLEMENTED + headless-verified** (`Plans/pattern_pitch.md` → DONE) — **pattern subsystem now feature-complete (P1+P2+P3).** `PITCH_MODE_PATTERN` scale-degree stepper. types.h: `PITCH_MODE_PATTERN` enum value (deferred from P1). grain.c: pitch case in `scheduler_trigger_grain` reading `pattern[pitch_pattern_slot].cached_value` as a degree with **wrap+octave** (`((deg%count)+count)%count` + `floorf(deg/count)*12`) — the wrap `sample_scale_semitones` lacked — rest-holds, reuses `last_semitone`/±4.0 clamp. ligase~.c: `pattern pitch` commit sets `pitch_pattern_slot`+`mode=PITCH_MODE_PATTERN`; `pattern_clear pitch`→`mode OFF`+slot −1; `ligase_pitch_mode` widened 0-5 (+"pattern"); outlet-3 note-change includes PATTERN; `pattern_debug`-gated pitch trace. **Verified headless** (record+play to granulate, scale `0 2 4 5 7 9 11`): wrap+octave (degrees 0..7 → 0,2,4,5,7,9,11,**12**), alternation (one degree/cycle 0/7/12), nested `[0 [4 7]]` (0,7,12), mode plumbing (5 ok, 6 rejected, auto-set, clear→off), scale-mode no regression. `make clean && make` warning-free; `test_delay.pd` clean. **Subsystem remaining: owner ear-test in plugdata** (not agent work). | SLB |
| 2026-06-24 | 48 | **Pattern P2 IMPLEMENTED + headless-verified** (`Plans/pattern_modulation.md` → DONE). `RAND_TYPE_PATTERN` as a modulation source on any `get_param_range_by_name` target. types.h: added the `RAND_TYPE_PATTERN` enum value (deferred from P1) + `saved_rand_type`/`saved_rand_instance` on `param_range_t`. grain.c: `case RAND_TYPE_PATTERN:` in both `sample_param_range` and `sample_scale_semitones` (reads `pattern[rand_instance].cached_value`, bypassing the 0..3 clamp); `default_range` initializer extended. ligase~.c: `pattern_alloc_param_slot()` (reuse-or-first-free, slot 7 reserved for pitch); `ligase_pattern` first arg generalized (param-name→auto-slot+attach / `pitch`→slot 7 / numeric→load-only); attach saves prior source + sets PATTERN/instance/enabled + resets min==max→[0,1]; `ligase_pattern_clear` generalized (slot|pitch|name-restore); `pattern_N` branch + `is_pattern`-guarded 0..3 relaxation added to BOTH `ligase_rand_type` and `ligase_pitch_rand_type`; print fixes — `param_range` dump labels `"pattern"`, and `get_rand_type_name` SAW/SINE/SQUARE mislabel-as-"none" gap fixed (+`"pattern"`). **Verified headless** via `modout`→`[change]`→`[print]`: even map (100/500/900), nested+invert (80/60/20), clear/restore (sine resumes, label correct), slot ≥4 (`pattern_6`→slot 5 reads 20/80), min==max reset, independent slot allocation (modout2=slot0, smear_frequency=slot1), `unknown parameter 'smear'`. `make clean && make` warning-free; `test_delay.pd` regression clean. Next: P3. | SLB |
| 2026-06-24 | 47 | **Pattern P1 IMPLEMENTED + headless-verified** (`Plans/pattern_notation.md` → DONE). Built the notation parser + recursive nesting + the fifth free-running BPM cycle clock across the four gates, `make clean && make` warning-free at each. types.h: `pattern_step_t`/`pattern_table_t`/`pattern_node_t` + `PATTERN_*` caps, `pattern[]`/`pattern_phase[]`/`pattern_cycle_index[]` in `perlin_state_t`, `pitch_pattern_slot` in `pitch_control_t`. ligase~.c: `ligase_t` cycle fields + `pattern_debug`; `ligase_recompute_cycle()` wired into `ligase_bang`; `pattern`/`pattern_cycle`/`pattern_clear`/`pattern_debug` handlers + registration; two-stage `ligase_pattern` (tree→flat-table, validate-then-commit, publish `step_count` last); per-block phase-advance loop in `ligase_perform`. grain.c: `pattern_eval_slot()` (non-static + extern) + `pitch_pattern_slot=-1` init. **8/8 acceptance criteria pass** (even timing 2.0s/4=0.5s; `4/4 3/8`=2.75s; nested `[0.2 0.4] 0.8`=¼,¼,½; weighted `0.2@3`=¾,¼; alternation one-per-cycle default-cycle; `pattern pitch < 0 4 7 >`→slot 7 raw degrees; clean errors + prior-slot-preserve; bpm=0 guard no-NaN/no-crash). Regression: `test_delay.pd` clean, WAV intact. **Sanctioned GATE-A deviation:** `RAND_TYPE_PATTERN`/`PITCH_MODE_PATTERN` enum values DEFERRED to P2/P3 (added atomically with their switch cases) — adding them in P1 dangled `-Wswitch` warnings with no P1 user; deferral was the GATE-A alternative. Test patches in `/tmp/pat_tests/`. Next: P2. | SLB |
| 2026-06-24 | 46 | Pattern subsystem scoped (TidalCycles `<,>` notation + `[4/4,3/8]` quantization cycle, owner request). Authored 3 execution plans via a research+design workflow (TidalCycles/Strudel mini-notation research + file:line grounding of the modulation/BPM-quant/pitch-scale/Pd-parse internals, two competing architectures synthesized, then adversarially verified against the tree — which corrected fabricated `class_addmethod` line refs, a wrong param name `smear`→`smear_frequency`, and missing `bpm==0` guards). Hybrid architecture: a parse-time recursive tree flattened to a flat weighted step table, attached via a new `RAND_TYPE_PATTERN` (reuses `param_range`'s invert/map/slew tail, zero per-param plumbing) and a `PITCH_MODE_PATTERN` sibling; a *fifth* free-running BPM-locked cycle clock built from the existing `(60000/bpm)*4` grid math; comma-free wire syntax (Pd binbuf eats `,`/`;`, so the cycle rides a dedicated `pattern_cycle 4/4 3/8` selector and values are bare space-separated atoms). **P1** `Plans/pattern_notation.md` (parser+clock+nesting), **P2** `Plans/pattern_modulation.md` (`RAND_TYPE_PATTERN` attach), **P3** `Plans/pattern_pitch.md` (`PITCH_MODE_PATTERN` scale-degree stepper w/ wrap+octave). Placed in §2 ON DECK + §4a; dependency chain P1→P2→P3. Syntax clarified to canonical Tidal mini-notation: sequences are space-separated, and `,`=stack (parallel voices) is **not applicable** to a single scalar/pitch-over-time pattern, so it is intentionally unsupported (owner caught the stack semantics). **GATE A owner-approved across all three** (recommended options: `PATTERN_SLOTS=8`, reject-not-truncate 64-leaf cap, strict denominator set, `enabled`-on-attach, pitch dedicates the last slot); now gated only on build order. No code yet. | SLB |
| 2026-06-16 | 0 | Template instantiated for ligase~. Added M1 (PDF manual regeneration, `Plans/pdf_manual_regeneration.md`); manual-content-edits on deck (§2); build-naming cleanup to backlog (§4). | SLB |
| 2026-06-16 | 1 | M1 advanced: GATE 0 cleared (pandoc+weasyprint installed), Steps 1 & 3 done (Markdown master `docs/ligase_manual.md` + `make manual` build), PDF regenerated & current. M1 now at GATE 4 (txt-fate decision). §3 unblocked. | SLB |
| 2026-06-16 | 2 | M1 DONE: GATE 4 cleared — `src/ligase_manual.txt` deleted per user; single-source pipeline live. Manual-content edits (§2) now unblocked, awaiting go-ahead. | SLB |
| 2026-06-16 | 3 | Modulation-coverage scan (3 agents) recorded. Authored `Plans/manual_content_edits.md` with Worklist A (5 missing modulatable targets: organize/sos/env_skew/gdelay_feed/gdelay_mix; `param_invert` undocumented; phantom `modout_*` messages; stale "21" count). §2, §4a, and the SLB lane updated to point at it. | SLB |
| 2026-06-16 | 4 | Two major runtime bugs scoped (2-agent root-cause scan) and placed at top of §1 AGENT lane: B1 sample-rate/buffering (`Plans/sample_rate_buffering.md`) and B2 reel load/save on macOS (`Plans/reel_io_macos.md`). Both at GATE A (approval). §0 map re-prioritized: bugs above doc work. | SLB |
| 2026-06-16 | 5 | `TEST_PLAN_MACOS.md` authored (Focusrite + SR-sweep). B1 Step 1 implemented (F1 delay realloc, F2 bencina recompute, F3 distortion coeff+state reset, F6 blocksize-bail silence, + change-gated `ligase_set_sample_rate` dispatch in `ligase_dsp`); `make clean && make` clean; headless smoke at 96 kHz passes. B1 → IN-PROGRESS at GATE B. (Env notes: installed Pd 0.51.1 vs Makefile 0.53 path; stale `src/*.o` need `make clean`; Focusrite not yet enumerated by CoreAudio.) | SLB |
| 2026-06-16 | 6 | B1 Step 1 functionally verified headless (Tier-1) at 44.1/48/96 kHz: `test_delay.pd` (6 s tap correct, 96 k clamp gone) + `test_dist.pd` (resonant distortion bounded, no NaN). Focusrite (Scarlett 2i2) now enumerated. Remaining: Tier-2 audible tests (user) + Step 2 reel/WAV SR (reel-sizing decision still open). | SLB |
| 2026-06-16 | 7 | B3 added + FIXED: empty-reel OVERDUB recorded silence when SOS up (recorded `input×(1−sos)`; pre-existing, NOT a B1 regression — recording code untouched by B1). Fix in `reel.c`: per-record `initial_length`; virgin samples capture full input regardless of SOS, SOS-crossfade only for genuine overdub onto existing content. Verified headless (sos 1.0 silent→full). Tests `test_rec.pd`, `test_input.pd` added. Awaiting user hardware confirmation (live input via Focusrite, `record 1` with SOS up). | SLB |
| 2026-06-16 | 8 | B3 CLOSED — NOT A BUG. Owner confirmed SOS is designed to attenuate the initial recording too; OVERDUB `input×(1−sos)` is intended. Seq-7 "fix" reverted in full (`reel.c`/`types.h` restored). Build clean; B1 changes remain intact and untouched. | SLB |
| 2026-06-22 | 45 | Catch-up + housekeeping + merge. (a) Logs B22–B35 (the 2026-06-19→22 arc; full detail in the §1 table): stut malloc-garbage param_range root cause + inlet/headless epsilon convention + layering voice-pool + length system, ending in a signal-driven *mapped* Stut (B22–B28); DD-4 `delay_glide` (B29); the Bencina rebuild — clip→tanh soft-limit, plain-delay→granular cloud via per-grain position scatter, per-grain random pan as a modulation target, then made the smoothing OPT-IN (`bencina_spread`/`bencina_edge`) to preserve the grainy character the owner wanted, restored the level, and exposed `bencina_level` (B30–B35); plus the Makefile header-dependency fix (B26) that had been silently masking earlier work. (b) Housekeeping: archived the 3 completed plans (B1/B2/M1) to `Plans/completed/` and corrected their stale SCOPED/IN-PROGRESS headers to DONE; relabeled §1 as the completed changelog (active queue now empty); refreshed §0 + Queue Seq. (c) Merged `fix/audio-engine-and-manual` → `main` (fast-forward, 74 commits). Remaining project-wide: user hardware/ear sign-off; §2 manual-content remainder + §4 build-naming stub. | SLB |
| 2026-06-19 | 44 | B21 FIXED — in headless 1, inlet 11 was overriding the stut_reps message ('only one stut'). The Seq-36 inlet-11→stut_reps read wasn't headless-gated, so a delay-time value on inlet 11 (shared with gdelay_time) was read as the count every block, stomping the message even in headless 1. Fix: inlet 11 drives stut_reps ONLY in headless 0; headless 1 ignores it (message authoritative — owner's point: shouldn't need to disconnect in headless 1). Verified headless 1 → message wins (count 8), headless 0 → inlet drives (count 2). Manual updated. | SLB |
| 2026-06-19 | 43 | B20 DONE (owner): stut repeated-unit length was hardwired to the spacing. Added `stut_length_mode` (0=independent, 1=grainsize), `stut_length` (ms), and `stut_length_quantize`/`stut_length_quant` (BPM note-grid, same pattern as delay/iot quant). Slice length now decoupled from spacing (gated/gapless/overlap). grain_delay_stut_trigger gained a play_length_samples arg; ligase_stut computes the length per mode. Verified: ms (30/250), grainsize (100), and BPM-quantized (1/8 @120 = 250ms). Manual + quick-list updated. | SLB |
| 2026-06-19 | 42 | B19 DONE (owner): smear params are now param_range modulation targets. Added smear_frequency/resonance/stages/feedback ranges (scheduler struct + scheduler_create init + perform sample/apply + get_param_range_by_name + rand_type all-list). grain_smear_t is opaque in ligase~.c and sample_param_range ignores base_value when enabled, so a 0 placeholder base is used (sampling only inside the enabled guard). Verified: all 4 accept param_range/rand_type (no 'unknown parameter'); modulated freq+feedback bounded/non-silent (max 0.20). Manual modulation list updated. | SLB |
| 2026-06-19 | 41 | B18 DONE (owner-approved tuning): Bencina delay was too subtle + inlets 11/12 felt dead. Diagnosed: 11/12 measurably work but the cloud is quiet; prominence is dominated by uncompensated grain DENSITY (default ~2× overlap → subtle; dense → rails at 13.8). Fix (`grain_delay_bencina.c`): overlap-normalize the grain sum (density→texture not level), add 1.8× output-only makeup (feedback path stays unity → stable), denser default (spacing 25 ms). Verified: wet ~1.8× louder, 11/12 clear range, dense no longer rails (13.8→0.47), full-engine fb 0.95 bounded (max 0.24, non-silent). `BENCINA_WET_GAIN` tunable; owner ear-test. | SLB |
| 2026-06-19 | 40 | B17 FIXED — intermittent "can't record at init" (empty splices, silent engine w/ Moog meter-blips+clicks, restart-to-clear). Root cause: `reel_set_sample_rate` wiped the reel (length/count/splices→0) on a rate change; plugdata/JUCE re-reports the sample rate after instantiation, so a rate report after the user recorded destroyed the take. Ruled out perform statics (benign) + constructor (sound) + headless recording (reliable) first. Fix (`reel.c`): preserve recorded samples + splice markers across a rate change (memcpy, cap to new capacity) instead of wiping. Verified standalone (content+splices survive 48k→44.1k) + record/play regression (RMS 0.48). Host-timing trigger not reproducible headless; owner to confirm. | SLB |
| 2026-06-19 | 39 | B16 FIXED — `headless 0` silenced playback/recording. Owner's repro (noise in, recsplice→record0→play, flip headless 0) reproduced (after-flip RMS 0→). BISECTED: not a recent regression (pre-change build 4d6fd4c silences identically; longstanding headless-0 behavior, NOT the recsplice/stut/bencina/reel/envelope work). Root cause: `amplitude` (inlet 21) was the lone engine-critical param still honoring a literal 0 in headless 0 (grainsize/iot/speed already ignore bare 0); unconnected amplitude → grain level 0 → engine silent. Fix: amplitude ignores an exact-0 reading in both modes (like grainsize/iot/speed). Verified after-flip RMS 0.25 (playback survives). | SLB |
| 2026-06-19 | 38 | B15 FIXED — delay instability (owner: high feedback/repeats on delay-containing splices → dropouts / sub-meter volume / blow-up→silence). Root cause: the reel recording write (`recorder_process`) was the one stage with NO clamp and NO NaN flush. NEW_SPLICE records the SOS monitor (±4 when delay is hot); that baked into the splice and re-granulating (overlap multiplies) + recording again escalated unboundedly → Inf/NaN in the reel → blow-up/silence. Fix: firewall the reel write — NaN/Inf→0 + hard-clamp ±1 (transparent below full scale; caps the reel so the record→granulate→record loop can't grow). Verified headless under hot stress (×3 in, fb 0.98, moog res 3.8): reel bounded (avg 0.89), output finite \|max\|=0.9999, no NaN, non-silent. Latent distortion-state gap noted (stable one-poles, sanitized input → can't trigger; left). Owner: rebuild + retest the high-feedback / delay-in-splice scenario. | SLB |
| 2026-06-19 | 37 | B14 FIXED — Bencina & Stut produced no wet (owner: "only DD-4 works, mix is just an attenuator"). Isolated each mode (standalone harness, wet@mix=1). TWO bugs. Bencina: splice-wrap (the de-facto-forced default — `trigger` hardcoded wrap_mode=1, making `bencina_wrap` inert) remapped the grain read into REEL splice coordinates to index the separate 9.5 s delay buffer → read silent/unwritten regions for any splice not starting at buffer pos 0 → 0 wet. Fixed: wrap in delay-buffer space relative to the write head; `default_wrap_mode` (0=global straight delay, default) honored by trigger + set by `bencina_wrap`; loop mode (1) confines to recent splice_length samples. (Bencina also needs delay_time>0.) Stut: single-sample impulse per repeat → ~0 wet; now replays the captured slice (length=spacing, edge-faded) = a real stutter, no new param. Verified headless: all modes wet incl. offset splices (Bencina 0→1.06, Stut 0→0.82); mode-switch smoke clean. Manual updated. Owner: rebuild + retest Bencina (with a delay time set) and Stut. | SLB |
| 2026-06-19 | 36 | Feature (owner): inlet 11 was inert in Stut mode (the delay-time inlet has no meaning there — stut reads captured positions). Repurposed it to set `stut_reps` (TidalCycles `count`, 1-16), mirroring how inlets 12/13 become stut_reduction/stut_spacing in Stut. Guarded so a 0/unconnected inlet is ignored (won't stomp the stut_reps message/modulation); setter clamps 1-16. Confirmed first that stut already implements the full TidalCycles model (count/feedback/time + delay_quant grid quantization) — the earlier "stut_length/overlap/segment" idea was dropped as NOT part of stut. Updated `get_inlets` (inlet 11 shows stut_reps in Stut) + manual (inlet-11 note, per-mode table, active-inlets prose, footnote). Verified headless: Stut + sig~ 9 → inlet 11 → stut_reps=9; DD-4 + sig~ 9 → gdelay_time=9.000 (unchanged). | SLB |
| 2026-06-19 | 35 | B13 FIXED — random SIGSEGV crash. Owner crash log: `envelope_sample` ← `grain_delay_bencina_process` ← `grain_delay_process` ← `ligase_perform` (audio thread), after changing envelope type while in Bencina delay mode. Decoded the faulting instruction (`mulss xmm1,[rax+rcx*4+4]`, rax=0x04000000, rcx≈795903 → fault 0x04309c00) = ~795k OOB index ⇒ garbage `env->length` ⇒ dangling envelope pointer. Root cause: the `envelope` message did free+recreate (`envelope_destroy` then `envelope_create`) and updated `scheduler->envelope` but NOT `delay_bencina->envelope` (Bencina caches the pointer) → use-after-free. Fix: new `envelope_set_type()` rebuilds the table IN PLACE (same struct, like `envelope_set_skew`); handler calls it instead of free+recreate, so the struct pointer never changes and no cached holder can dangle. Verified headless (6 s Bencina + envelope-type cycling, clean exit). Owner: rebuild + retest delay-mode experimentation. | SLB |
| 2026-06-19 | 34 | DOC FIX (owner audit): the manual's signal-inlet numbers were wrong/misaligned. Audited every "Signal inlet" note in `docs/ligase_manual.md` against the authoritative code map (perform `w[]` unpacking + `get_inlets`; inlet N = `w[N+1]`). Fixed numbers: grainsize 2→3, iot 8→9, maxgrains 9→10, gdelay_time 10→11, env_skew 18→20, amplitude 19→21, moog_resonance 16→17, moog_mix 17→18. Re-attached the shifted delay notes so each sits on its own param with the right number: inlet 12 = feedback/stut_reduction, 13 = tone/stut_spacing, 14 = mix (all modes). Removed phantom signal-inlet claims from message-only params (distortion_enable, saw_cycles, saw_depth — none have signal inlets; saw_* also aren't param_range targets). Gave pan its real inlet 22. Re-audit: all 14 present notes now match code; 0 stale numbers. KNOWN GAP (pre-existing, left as-is): speed(5)/organize(6)/scanrate(7) have no signal-inlet notes. PDF NOT regenerated (holding per earlier "no pdf yet"). | SLB |
| 2026-06-18 | 33 | B12 follow-up #2 (owner): recinput ALSO must keep the playing splice playing through the take — owner heard the current splice stop and the live input become granulated mid-recinput, snapping back on stop. Cause: recinput still planted its marker at STOP, so the current (last) splice grew into the take and the granulator read the fresh raw input. Fix: recinput now plants its boundary marker at START too (`ligase_rec_input`), pinning the current splice exactly like recsplice; stop-handler simplified (no marker created on stop — both modes' marker is already planted). recinput and recsplice are now fully unified — both pin at start, both honor `splice_jump`, differing ONLY in what's recorded (recinput=raw input, recsplice=SOS monitor mix). Verified headless: during recinput the current splice stayed pinned `0–16832` while the reel grew to `48512` (loop keeps playing, take on fresh tape); 2×2 matrix + overdub-makes-no-splice still pass. Build clean. | SLB |
| 2026-06-18 | 32 | B12 follow-up (owner correction): `splice_jump` (jump_to_new) is a SHARED interface setting that must govern recinput AND recsplice identically. Seq 31 made recsplice honor it but left recinput unconditionally jumping → they diverged (recsplice stayed, recinput moved). Fixed: unified the `record 0` stop-handler so both modes run the same jump_to_new branch (0=stay default, 1=jump+reposition). They still differ only in marker-plant timing (recsplice=start to pin against feedback, recinput=stop). Verified headless across the 2×2 matrix: splice_jump=0 → both "staying on 0"; splice_jump=1 → both "jumped to new splice 2". Build clean. | SLB |
| 2026-06-18 | 31 | B12 FIXED: `recsplice` behaved like overdub (TLA into the current splice) + a quiet wrong copy in the new splice. Root cause: `splice_get_bounds` sets the LAST splice's end = `reel->length` (`splice.c:95`); after any take `current_splice` is the last splice, so as recsplice extended the reel the current splice grew to swallow the tape being recorded → the granulator read its own freshly-recorded `out_*` = feedback/TLA, and the marker carved at stop held the SOS-attenuated tail. Fix (`ligase~.c`): plant the new-splice boundary marker at recsplice **start** (`ligase_rec_splice`) to pin the current splice's end; keep granulating the existing splice during the take; on stop honor the existing **`jump_to_new`** flag (0=stay default, 1=switch+reposition, same path as `ligase_add_splice`). recinput/overdub/monitor chain untouched. Owner-confirmed mode model first. Verified headless: current splice pinned `0–16768` while reel grew to `77760` (take on fresh tape); jump_to_new=1 switches to `16768–77760`; overdub spawns no splice. Also scrubbed stale "Fog" comments in `ligase~.c` (effect is the smear). Build clean. Owner ear-test on hardware pending. | SLB |
| 2026-06-18 | 30 | Docs follow-up to Seq 29 DONE: rewrote all fog sections of the manual as the allpass smear (FOG section → SMEAR, message quick-list, inlet-15 entry, defaults, signal chain, modulation list), regenerated `ligase_manual.pdf` via `make manual` (0 "fog" tokens in md or pdf); removed the fog FFT config (fft_size/overlap_factor/fog_pool_size) from `ligase.conf` (only max_grains remains). Smear ear-test/tuning by owner still open. | SLB |
| 2026-06-18 | 29 | B9/B11 RESOLVED by DECISION: fog removed entirely, replaced by an allpass "smear" (owner's call, ref: Smoothie Audio Smear). The FFT fog was a persistent CPU/quietness/sound sink (Seq 18–28); the profiled cost was its per-bin polar transcendentals (Seq 28). New `grain_smear.{c,h}`: time-domain cascade of up to 48 2nd-order allpass sections + global feedback (unity-gain, |fb|<1 stable), controls `smear_frequency/resonance/stages/feedback` + mix on inlet 15. Deleted grain_fog.{c,h} + vendored kiss_fft/kiss_fftr (fog was their only user → also kills the FFT symbol-collision surface) and all fog refs across ligase~.c/grain.c/types.h/Makefile (0 "fog" tokens remain). Builds clean; smoke test loads/runs/tears down OK. Owner: ear-test the smear + tune defaults (freq 800, res 0.7, stages 12). Manual still documents fog — needs updating. | SLB |
| 2026-06-18 | 28 | B11 ROOT CAUSE (real this time, from a CPU sample on the owner's box): host is **plugdata 0.9.2** (Intel), not vanilla Pd. Audio thread: of 748 DSP samples, 735 = ligase_perform, ~700 = fog `process_fft_frame` — and NOT the FFT (kiss_fft ~15). The cost is the per-bin polar round-trip's libm transcendentals (atan2f/sinf/cosf/tanhf, 513 bins × 2ch × every hop; leaves in libsystem_m). Scales with the interface sample rate → invisible in the 48k sandbox; and the owner's sample shows transcendentals at ~95% of fog vs ~17% in-sandbox → subnormal inputs (quiet live mic) hitting slow microcode. FIX: fast inline approximations for atan2/sin/cos/tanh (output RMS unchanged, diff RMS ~0.0019) + flush subnormal FFT bins before polar. Headless x86 ~22% cheaper; on hardware where transcendentals dominate the win should be much larger. Owner: rebuild + re-sample to confirm. | SLB |
| 2026-06-17 | 27 | B11 still OPEN; bad turn reverted + cleanup. ARM-denormal theory was WRONG — owner is on an INTEL Mac (the x86 FTZ path is already active for them). The speculative ARM64 `LIGASE_FLUSH_DENORMALS` branch (unverified inline asm, added on that wrong premise) has been REVERTED — `ligase~.c` macro is back to the original x86-only form. Headless on x86 I cannot reproduce the creep even in the owner's exact scenario (`sig~`→inlet 15 on silence ≈ 2.4% CPU, fog ~6%, no creep), so the cause is environment-specific and not yet identified — needs to be reproduced on the owner's Intel box (Pd version/build/CoreAudio realtime). CONFIRMED real fixes that stand: Seq-26 post()-flood removal (turned instant adjustment-runaway into steady CPU), B10 NaN firewall, fog-pool scratch pre-alloc. Also removed the scratch fog test patches created during the (unwound) fog investigation. | SLB |
| 2026-06-17 | 26 | B11 added: "any fog adjustment → CPU runaway." NOT reproducible headless — correct patch sweeping fog params @500 msg/s + 60s steady fog both run realtime, no creep (an earlier "reproduction" was a malformed test patch, discarded). Cause hypothesis (strong, unverified): every fog param handler called `post()`; in the Pd GUI each post repaints the console, so a slider drag (100s/s) chokes the main thread → CPU spike. Invisible headless (stderr cheap). FIX: removed post() from all fog param handlers. Same pattern in ~100 other handlers (speed/grain/delay/moog/…) — sweep HELD pending owner confirmation this fixes fog. Owner: rebuild, test adjusting fog. | SLB |
| 2026-06-17 | 25 | B9 REVERTED to baseline. The complex heterodyne filter (Seq 23) regressed badly by ear: thin/scratchy, fixed-pitch resonance ring, too quiet, CPU creep→100% (kills session). All six session fog rewrites were rejected; designing the spectral character blind (no ear-test) failed repeatedly. Reverted `grain_fog.c`+`types.h` to c2788ff = original watery algorithm (smear + resonant temporal mag filter + phase-delta lowpass) + B8 loop-safety + dreamy defaults; kept B10 firewall. Net: removes the thin/scratchy/fixed-pitch/CPU regressions; original "too quiet" tradeoff returns (inherent — watery character = the phase processing that cancels/quiets). B9 now BLOCKED on owner decision: accept watery-but-quiet baseline, or tight ear-in-the-loop tuning, or leave fog and move on. | SLB |
| 2026-06-17 | 24 | B10 added + FIX (safety net): repeated report "monitor+SOS working, record, stop → silence and no monitor, nothing restores it" (all record modes; on the empty-reel→content transition that engages the granular path). Mechanism: `constant_power_mix` does in*a + delayed*b and NaN*0==NaN, so one non-finite granular sample kills even the 100%-dry monitor; if it reaches the reel/effect buffers it self-sustains. Fix: `ligase_sanitize_pair()` firewalls temp_* (post scheduler) and delayed_* (post delay) → dry monitor always survives, reel can't be poisoned. Could NOT reproduce headless (noise, 44.1/48/96k); pending hardware confirm. Root NaN source still unidentified — firewall guarantees the catastrophe can't happen but the granular path may still glitch-to-silent if a transient persists in an effect buffer. | SLB |
| 2026-06-17 | 23 | B9 redesign (owner: prior fog "does not sound good"; referenced Grainstorm's spectral filter). Researched it: Grainstorm "slows spectral evolution by filtering each FFT channel with resonant low pass filters" — i.e. filters the COMPLEX bins, not magnitude/phase. ligase's polar processing was the root of the bad sound (nonlinear, breaks mag↔phase). Rebuilt fog: per-bin 2-pole resonant lowpass on the complex channel, with HETERODYNE (rotate bin to baseband before filtering, since 4× overlap spins the bins) + per-frame energy restore. Removed all polar machinery (smear/mag/phase/vocoder/motion); `fog_mag_cutoff` 0.1–20 Hz + `fog_mag_resonance` are the live controls; smear_*/fog_phase_cutoff inert. Headless: noise −2.9 dB, tones ~−10 dB, no NaN, one realization dips (residual OLA coherence). NEEDS EAR-TEST — headless can't judge musicality; tuning (cutoff/resonance/default) handed to owner's ears. Possible next: non-overlapping FFT (true Grainstorm framing) if overlap artifacts persist. | SLB |
| 2026-06-17 | 22 | B9 iterated (owner: magnitude-only haze sounds "different character"; chose "richer magnitude haze"). Added magnitude-domain spectral MOTION: a broad ripple (depth 0.30, ~2.5 cycles) swept slowly (~0.15 Hz, R offset for width) — phase untouched so OLA stays coherent + per-frame norm holds level ⇒ moving spectral coloration, not level wobble. Headless: noise wet −0.6 dB, 220 Hz −1.6 dB; 300–900 Hz band sweeps ~±20% over the LFO period (motion measurable). Known residual dip unchanged (not worsened). depth/rate/cycles tunable; could wire rate to the inert `fog_phase_cutoff`. Owner ear-test pending. | SLB |
| 2026-06-17 | 21 | B9 iterated: "fog too quiet, not noticeable until high wet %." The Seq-20 wet makeup gain only matched when fog was ramped mid-stream; with fog ON from the start it mistracked (~26 dB down). Replaced with deterministic per-frame spectral-energy normalization (rescale processed magnitudes to the input frame's energy; phase untouched ⇒ coherent OLA at dry level); makeup machinery removed. Headless (averaged): pure OLA exact every run; effects on → noise −0.7 dB, 220 Hz −1.7, 440 Hz −4.0 (was 26–37). Known residual: magnitude-mod + fixed phase isn't guaranteed OLA-coherent ⇒ occasional few-dB dips on granulated tones (not a dropout). Also logged: one unreproduced "silence after recording into a splice" report (owner watching). Owner ear-test pending. | SLB |
| 2026-06-16 | 20 | B9 added + FIXED: "adding fog drops output volume." A first pass (magnitude soft-limiter normalization + wet makeup gain) only matched NOISE; tonal material still collapsed ~37 dB. Real cause = the temporal PHASE filter broke overlap-add coherence (4x overlap) so coherent tones cancelled. A phase-vocoder rewrite cancelled intermittently (~1 run in 8, inter-bin decoherence), so phase is now left UNTOUCHED (deterministic full-level OLA). Fog haze is magnitude-only: energy-preserving smear + temporal magnitude filter, with the makeup gain (cap 6x) covering the residual variance loss. Verified headless (`test_fog_const.pd`, averaged/deterministic): noise within ~0.1 dB, tones ~0–5 dB, no collapse (was ~37 dB). `fog_phase_cutoff` now inert (manual/setter to reconcile). Limiter-normalization fix from the first pass retained. Owner ear-test pending; phase-locked vocoder a possible future enhancement. | SLB |
| 2026-06-16 | 19 | Refreshed §0 strategic map + §1 AGENT-lane summary to reflect reality: all bug work (B1/B2/B4/B5/B6/B7/B8) + manual + fog retune DONE and verified headless; agent backlog empty; remaining is user hardware sign-off + push/PR. (Per-item rows + §6 were already current; only the top blurb was stale.) | SLB |
| 2026-06-16 | 18 | Fog defaults retuned for musicality (smear_bins 8->3, smear_onset 1.0->0.8, mag_cutoff 2.0->2.5 Hz, mag_resonance 1.0->0.5, phase_cutoff 2.0->3.0 Hz, stereo shared->independent) — a smooth, lush, dreamy haze vs the old washy/ringy/mono-ish default. Manual defaults synced + PDF regenerated. Aesthetic; owner to confirm by ear. | SLB |
| 2026-06-16 | 17 | B8 added + FIXED: persistent 100% CPU traced (3-agent hunt: playhead/delay/fog) to unbounded audio-thread `while`-wraps + unbounded filter state. Replaced playhead wraps with O(1) `wrap_to_splice` (kills the zero-length-splice scanning hang); added delay `buffer_size<=0` guard + fmodf read-pos + NaN flush (incl. Bencina lpf); replaced fog phase-unwrap `while` with an fmodf fold (kills the Inf-delta infinite loop), bounded `phase_prev` + mag-filter state, and added an isfinite firewall before the OLA buffer. Builds; delay/TLA regression clean. Probable true cause of the stuck-100% (supersedes B6). | SLB |
| 2026-06-16 | 16 | B7 added + FIXED: fftease load conflict + fog malfunction + fog-inlet CPU spike traced to a symbol collision — ligase exported all non-static functions (incl. vendored `kiss_fft`) into Pd's flat namespace. Fix: `-fvisibility=hidden` + export only `ligase_tilde_setup`. Verified only `_ligase_tilde_setup` exported (0 internal leaks); loads/runs. This is the likely real cause of the fog CPU spike (B6's distortion-denormal attribution was probably wrong; B6's FTZ/DAZ retained as general denormal hygiene). Owner verifies on hardware. | SLB |
| 2026-06-16 | 15 | B6 added + FIXED: CPU creep over minutes = x86 denormals. Found the gap (distortion IIR states unflushed while delay/fog/moog were protected); fixed comprehensively with hardware FTZ+DAZ in `ligase_perform` (`LIGASE_FLUSH_DENORMALS`, x86-guarded). Builds + runs; `stmxcsr` confirmed in binary. Multi-minute creep can't be reproduced headless (timescale matches slow feedback→denormal decay); owner to verify on hardware. | SLB |
| 2026-06-16 | 14 | B2 polish + manual finish. B2: robust chunk-walking WAV parser (handles extra chunks / non-16 fmt / cue-after-data), 16-bit PCM accepted (→float), read/write error checks, specific failures via `pd_error` (`reel_io_strerror` + `reel_io_status_t`); is_path_safe relaxed to block only `../`. Verified headless (float round-trip w/ cue, 16-bit load, missing/non-WAV error messages). Manual: rewrote the SOS-mechanics recording prose to the mode-dependent behavior (recinput raw / recsplice what-is-heard / overdub TLA); regenerated PDF. B2 + manual content edits now DONE. | SLB |
| 2026-06-16 | 13 | Manual content pass + B1 Tier-2 note. Updated `docs/ligase_manual.md` to match the engine/record-mode changes (dropped "48 kHz fixed" → host-rate; rewrote recinput/recsplice/overdub-TLA; load notes any-rate + patch-relative) and applied Worklist A (added organize/sos/env_skew/gdelay_feed/gdelay_mix + modout1-4 targets; documented param_invert/param_slew/param_base_value; removed phantom modout_source/range; fixed source/param counts). Regenerated `ligase_manual.pdf`. B1 Tier-2: owner reports no clicks on a short Focusrite test (dropout symptom gone). | SLB |
| 2026-06-16 | 12 | B1 Step 2 DONE (reel-sizing = reallocate-by-runtime-SR, owner-approved): reel capacity now sized to the host rate and reallocated on SR change (`reel_set_sample_rate`, wired into `ligase_dsp`); WAV save writes the real rate; load accepts any rate (was 48k-only) with a speed-mismatch note. `reel_clear`/`recorder_process` use `reel->capacity`. Verified at 44.1/48/96 kHz: header rate correct, duration consistent, 96 k round-trip intact. B1 fully implemented; only Tier-2 audible (user) remains. | SLB |
| 2026-06-16 | 11 | B2 CORE FIXED: reel `load`/`save` resolve via the patch canvas (`canvas_makefilename`/`canvas_open`) — relative paths now anchor to the patch dir, not Pd's CWD; fixes silent load/save under a Finder-launched Pd.app. Stores `x_canvas` from `canvas_getcurrent()`. Verified headless with cwd≠patchdir; round-trip intact. Robust-parser/header-SR/error-routing follow-ups remain optional. | SLB |
| 2026-06-16 | 10 | B5 IMPLEMENTED (owner-directed): overdub Time Lag Accumulation. (1) Record head now loops WITHIN the current splice (`reel.c` — was running off splice_end and extending the reel); cross-splice is via `shift`. (2) Overdub records the granular playback fed back (so pitch/grain settings compound), with a sub-unity SOS-scaled feedback coefficient (`TLA_FEEDBACK_MAX=0.95`), the granular feedback clamped to ±1 before the coefficient (grain engine has no gain-comp), and a tanh output ceiling — stable, no runaway. Verified headless: loops in splice (reel stays 0.3 s), bounded (max 0.66, RMS 0.28, no rail); recinput/recsplice unaffected. Grounded in Morphagene TLA research. Ear test on hardware pending; `TLA_FEEDBACK_MAX` tunable. | SLB |
| 2026-06-16 | 9 | B4 FIXED: `recsplice` (NEW_SPLICE) now honors SOS-as-VCA. It had been merged with INPUT_ONLY (recorded raw input, SOS bypassed), contradicting design + manual. Both `sos_mode==1` record sites in `ligase~.c` now record the SOS-mixed monitor output (`out_*`) for NEW_SPLICE; `recinput` (raw) and `sos_mode==0` left untouched; OVERDUB unchanged. Verified headless (recsplice sos-VCA constant-power; recinput raw; overdub `1−sos`). Owner-confirmed intended behavior. | SLB |
